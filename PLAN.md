# 美业门店微信预约小程序详细设计方案

## 一、目标与已确认规则

在现有原生微信小程序与 CloudBase 示例基础上，建设可商用的单店预约系统，包含顾客小程序、店员与技师工作台、电脑管理后台。

实施结果已写入 `docs/预约小程序技术方案.md`，包含 MySQL 表结构、索引、接口、状态机、页面规格、部署和验收清单，并同步更新 `docs/CURRENT_STATUS.md`。当前 CloudBase SQL 基础设施、云函数读写链路和管理后台登录会话已落地；微信支付资质、真实交易和真实账号验收仍按清单待补。

| 项目 | 首版规则 |
|---|---|
| 服务分类 | 美甲、美眉、纹绣，每类包含多个小项目，每个小项目包含多个款式 |
| 预约单位 | 每单一个大项下的小项目和一个款式、一名技师、一位顾客 |
| 技师容量 | 同一技师同一时间只接待一人 |
| 开放范围 | 提前14天开放，至少提前1小时预约 |
| 支付 | 全额预付，下单占位5分钟 |
| 到店核销 | 店员或对应技师确认到店，随后开始服务 |
| 自动退款 | 预约开始30分钟后仍未核销，自动取消并发起全额退款 |
| 自主取消 | 到店核销前允许取消并全额退款 |
| 服务后退款 | 管理员处理，首版支持整单退款 |
| 获取积分 | 服务完成后，实付每满1元获得1积分 |
| 使用积分 | 20积分抵1元，每单最高抵项目金额10% |
| 经营统计 | 仅统计小程序订单 |
| 视觉 | 奶油白、低饱和玫瑰色、真实作品大图 |
| 云资源预算 | 固定19.9元/月，不自动升级；外部必要费用另计 |

预约、退款和积分参数后台可调整，修改只影响新订单。首版不提供组合项目、定金尾款、储值卡、线下消费补录和多店管理。

## 二、技术架构与微信接入

### 2.1 系统组成

- **顾客端**：原生微信小程序，延续当前工程；新增可复用组件及 TypeScript 类型约束。
- **管理后台**：Vue 3、TypeScript、Vite，适配电脑和平板；静态资源部署至当前环境可用的静态托管能力。
- **业务后端**：CloudBase Node.js 云函数，划分身份、目录、预约、支付、积分、管理、统计和后台任务模块。
- **数据库**：CloudBase 原生 MySQL 8.0，业务读写只经云函数，使用 InnoDB 事务保证预约、积分和资金状态一致。
- **图片**：CloudBase 云存储，发布时生成列表图和详情图，减少大图传输。
- **后台任务**：定时触发器配合持久化 `jobs` SQL 表，负责关单、退款、通知、对账和统计。
- **支付回调**：独立公网 HTTPS 入口，保留原始请求体用于验签，与用户接口隔离。

顾客接口通过可信微信身份上下文识别用户；服务端不接受客户端传入的 OpenID、角色、价格或积分余额作为可信依据。

电脑后台使用 CloudBase 用户名密码认证，不开放公众注册；首个已登录用户仅在 `staff_accounts` 为空时通过一次性初始化成为 `OWNER`，后续由店主授权。

### 2.2 微信支付 API v3

采用普通商户直连：

1. 使用营业执照完成小程序主体认证。
2. 申请商户号和小程序支付权限。
3. 完成商户号与 AppID 绑定。
4. 配置商户私钥、API v3 密钥及微信支付验签材料。
5. 云函数调用 `/v3/pay/transactions/jsapi`，返回调起支付所需参数。
6. 小程序调用 `wx.requestPayment`。
7. 服务端通过支付回调或主动查单确认实际支付结果。
8. 退款调用 `/v3/refund/domestic/refunds`，通过退款回调和查单确认到账状态。

这些权限及绑定属于官方接入前置条件。[微信支付接入准备](https://pay.wechatpay.cn/doc/v3/merchant/4015459512) · [小程序下单接口](https://pay.wechatpay.cn/doc/v3/merchant/4012791856)

支付请求、响应和回调均按 API v3 规范验签；校验商户号、AppID、订单号、金额和币种。密钥仅保存在服务端受控配置中。

**不以客户端支付成功提示确认订单，也不以退款申请受理成功显示“已到账”。**[退款申请说明](https://pay.wechatpay.cn/doc/v3/merchant/4012791903)

### 2.3 手机号与订阅消息

设计为：

- 用户可以直接浏览作品与项目。
- 首次预约时，用户主动授权手机号；服务端换取并绑定联系方式。
- 用户身份以微信身份为主键，手机号变更不转移历史资产，不根据相同手机号自动合并账号。
- 用户拒绝手机号授权仍可浏览；预约提交前需要完成联系方式绑定。
- 在明确的用户操作后申请订阅消息，记录授权结果；未授权不阻断支付。
- 通知包含预约成功、到店提醒、预约取消及退款结果，具体模板以小程序类目实际可申请内容为准。
- 不能将一次授权视为无限次通知许可；通知失败不回滚预约或退款。

本次微信开发文档正文抓取失败，手机号资格、收费、订阅模板和授权限制须在实现前通过官方文档及公众平台核实，并记录于方案的接入检查表，不能写成已经验证可用。

## 三、数据库、接口与核心业务

### 3.1 数据模型

所有金额使用整数“分”，积分使用整数；时间保存标准时间戳，营业日按 `Asia/Shanghai` 计算。业务对象落在 MySQL InnoDB 表中，每张表以 `id` 为主键，`data` JSON 列保存订单快照、排班数组等嵌套结构，`created_at`/`updated_at` 提供基础 SQL 索引。服务端 SQL 适配层使用参数化查询和事务，不向客户端暴露数据库连接。

| SQL 表 | 主要内容与约束 |
|---|---|
| `users` | 微信身份关联、联系方式、注册时间、状态；敏感联系方式加密保存 |
| `staff_accounts` | 管理端身份、角色、对应技师、停用状态 |
| `categories` | 三大项及展示分类、排序、启用状态 |
| `services` | 小项目名称、价格、服务时长、缓冲时间、介绍、所属大项 |
| `works` | 款式图片、分类标签、关联小项目、发布状态、排序 |
| `technicians` | 技师介绍、头像、技能项目、展示排序、启用状态 |
| `schedule_templates` | 每周营业与排班模板、休息时间 |
| `technician_days` | 技师每日班次、请假、占用区间、版本号；技师与日期组成确定性ID |
| `orders` | 用户、技师、项目快照、预约时间、价格快照、积分规则快照、业务状态 |
| `payments` | 商户订单号、微信交易号、应付金额、支付状态、查单时间 |
| `refunds` | 唯一退款单号、退款金额、原因、微信状态、重试记录 |
| `points_accounts` | 可用积分、冻结积分、版本号 |
| `points_ledger` | 获取、冻结、消耗、解冻、退还、冲回与人工调整流水 |
| `jobs` | 任务类型、业务ID、执行时间、重试次数、租约、执行状态 |
| `notification_records` | 模板、业务事件、发送状态及结果 |
| `audit_logs` | 操作者、对象、动作、修改摘要、原因和请求ID |
| `daily_metrics` | 每日成交、退款、服务收入、新老客及技师指标 |
| `settings_versions` | 门店资料、预约和积分规则的不可变版本 |
| `idempotency_keys` | 用户幂等键、订单 ID 和过期时间；用于防止重复下单 |

详细字段文档必须包含类型、必填性、默认值、读取权限、写入权限和数据保留规则。

业务查找键和 SQL 索引覆盖：

- 每张表的 `id` 是大小写敏感的主键，`created_at`/`updated_at` 建有基础索引。
- 技师日程、支付、退款、积分和幂等记录使用确定性 `id`，分别保证同一技师日期、支付单、退款单和幂等键不重复。
- 目录、订单和任务的业务字段保存在 `data` JSON 中，由服务端适配层校验后筛选；数据量扩大时再为高频字段增加生成列索引，不改变业务接口。

业务数据采用下架、停用和归档。执行过程中需要删除的本地文件均移入废纸篓。

### 3.2 防止重复预约

采用“技师每日占用 SQL 行＋短事务”：

1. 按项目时长与缓冲时间计算连续占用区间。
2. 按15分钟步长展示候选开始时间，区间采用左闭右开计算。
3. 创建订单时，在同一事务中读取排班、项目有效版本和积分账户。
4. 校验技能匹配、营业时间、请假、冲突和积分余额。
5. 原子写入占位、订单、积分冻结及后续任务。
6. 事务成功后才请求微信预支付。

不同技师的请求分散到不同文档，同一技师同一天的冲突通过事务控制。限制每位用户最多持有一个待付款占位，减少恶意占用。

排班调整也更新同一每日文档；存在有效订单时禁止直接缩短至冲突时段，管理员先处理受影响订单。

MySQL 事务在服务端执行，事务内使用行锁读取关键记录；事务内禁止调用微信支付等外部服务，必须提交预约和积分状态后再请求支付。

### 3.3 状态与资金一致性

预约、支付、退款分别记录状态，避免混用。

预约主流程：

`待付款 → 已预约 → 已到店 → 服务中 → 已完成`

分支包括待付款关闭、用户取消、超时未到店取消。取消后的资金状态可以仍为“退款处理中”。

关键规则：

- 付款超时先查单、关单，再释放占位；结果不明时进入补偿，不能直接假定未支付。
- 已释放占位后发现迟到支付，不恢复可能冲突的预约，自动发起退款。
- 用户取消、核销和超时任务争抢同一订单状态，只有一个合法状态转换生效。
- 超时退款依据服务器时间；任务延迟执行后仍按业务截止时间判断。
- 所有支付、退款及积分动作使用固定业务幂等键。
- 微信调用超时先查询结果，不生成新退款号盲目重试。
- 重复回调和乱序回调不得逆转终态或重复记账。
- 界面明确区分退款申请、处理中、到账和异常。
- 核销码短期有效且一次使用，截图转发不能绕过服务端状态校验。

首版不提供直接改期，采用取消退款后重新预约。

### 3.4 积分计算

默认以20积分为一个抵扣单位：

- 获得积分：`floor(实际微信支付金额分 / 100)`。
- 可抵扣单位：受可用积分和订单10%上限共同限制，向下取整。
- 每单位消耗20积分，抵扣100分。
- 下单冻结积分；支付确认后消耗；未支付关闭后解冻。
- 服务完成后发放积分。
- 整单退款成功后退还原消耗积分；已发放积分按原订单数值冲回。
- 若奖励积分已被使用，允许形成积分欠额，暂停继续抵扣，后续获得积分先补足欠额。
- 人工调整必须填写原因并保留审计记录。
- 首版积分不设过期，不引入积分失效批次管理。

示例：200元项目，使用400积分抵20元，微信支付180元，服务完成获得180积分。

### 3.5 接口划分

| 模块 | 核心接口能力 |
|---|---|
| 身份 | 登录、绑定手机号、获取个人资料 |
| 展示 | 大项、小项目、款式、技师详情 |
| 预约 | 选择大项和小项目后选择款式，再展示可选时段、服务端报价、创建订单、取消、订单详情 |
| 支付 | 获取支付参数、查询支付、支付回调、退款回调 |
| 会员 | 积分账户、流水、消费记录 |
| 工作台 | 我的排班、待到店订单、核销、开始及完成服务 |
| 管理 | 项目作品维护、技师排班、规则发布、退款、经营分析 |

报价接口返回报价标识、到期时间和规则版本；创建订单必须重新校验。统一返回业务错误码与请求ID，涵盖时段被占、报价变化、权限不足和系统繁忙。

## 四、页面、管理与经营分析

### 4.1 顾客端

底部导航采用：首页、项目、预约、我的。

- **首页**：门店信息、三大类别、分类作品、精选技师。
- **作品详情**：真实款式图、关联小项目、价格、时长和预约入口；直接携带已选款式进入预约流程。
- **项目详情**：小项目说明、价格和选择款式入口。
- **预约页面**：技师→日期→可用时间→积分与付款确认，持续展示总价、抵扣和实付。
- **预约记录**：待付款、待到店、服务中、已完成及退款状态。
- **个人中心**：积分、消费历史、预约记录、联系方式、隐私与客服入口。

空状态、图片加载失败、支付取消、网络中断和预约被抢均提供明确反馈。

### 4.2 视觉与适配

- 奶油白背景、深灰正文、玫瑰色主按钮，价格与重要状态保持足够对比度。
- 统一8点间距体系、圆角、按钮高度和字体层级。
- 作品使用统一裁切比例，列表加载缩略图，详情按需加载高清图。
- 使用弹性布局、`rpx` 和安全区适配，避免固定屏幕高度。
- 覆盖窄屏、常规屏、大屏、刘海屏及底部手势区域。
- 点击区域不小于44逻辑像素；支持系统字体放大后的换行。
- 不采用影响阅读和低端机性能的大量动效。

### 4.3 后台权限

- **店主**：全部业务设置、员工授权、退款、积分调整、数据导出。
- **店员**：预约管理、核销和服务操作，按授权查看必要联系方式。
- **技师**：仅自己的日程、相关预约及服务状态操作。

管理员可维护营业时间、每日排班、请假、项目技能、技师介绍和人数。人数由启用技师及其可用排班决定。

后台不能直接编辑订单实付金额或积分余额，必须通过有审计的业务操作完成。

### 4.4 统计口径

分别展示：

- 当日微信支付成功金额。
- 当日退款成功金额。
- 当日净收款＝当日收款－当日退款。
- 当日完成服务订单的实付收入。
- 新客、老客人数及占比。
- 预约量、取消率、未到店率、客单价。
- 项目及技师的成交额、服务次数、排班利用率。
- 积分发放量、抵扣量及抵扣金额。

以首次完成有效消费识别新客；统计按用户去重。历史退款影响原服务期收入时重算相关日报，同时计入退款发生日现金流，避免将两种口径混为一谈。

## 五、预算、安全、恢复与验收

### 5.1 固定19.9元预算

官方目前列出的个人版包含有限资源，且不提供数据回档；实际权益以购买页和控制台为准。[CloudBase 套餐说明](https://cloud.tencent.com/document/product/876/75213)

方案使用 CloudBase Serverless MySQL；不启用 Redis、预置并发或自动升级。微信支付手续费、认证、手机号能力等外部费用另计。

- 不使用面向全部用户的实时数据库监听。
- 页面分页、缓存静态目录、压缩图片，避免频繁轮询。
- 优先保留支付确认、关单、退款和对账资源，报表允许延迟。
- 资源使用70%告警、85%降低非关键刷新、90%停止新预约；能够自动读取的用量通过任务检查，其余由平台告警和管理操作补足。
- 是否能设置套餐外费用硬限制必须在控制台核实；不能仅依赖业务阈值保证账单绝不超额。
- 套餐额度耗尽或平台故障时，退款执行也可能延迟，不能承诺全天候即时退款。

### 5.2 安全

- MySQL 不向小程序客户端开放直连；业务数据只经云函数读写。
- 每个接口执行身份、对象归属、角色和状态检查。
- 金额、积分、预约截止时间均由服务端计算。
- 限制请求频率、分页大小、文本长度和上传类型。
- 限制富文本能力，防止管理后台存储型脚本注入。
- 公开图片与私有文件分离，上传权限受控。
- 电话号码日志脱敏；不得记录密钥、完整令牌和支付敏感报文。
- 管理后台开启平台支持的额外认证措施，限制登录失败重试。
- 退款、积分调整、授权及规则变更保留业务审计。
- 上线前检查依赖漏洞、密钥泄漏、越权和回调伪造。

安全目标是降低攻击面并验证控制有效，不承诺“绝对无法攻破”。

### 5.3 故障恢复

使用 CloudBase 托管运行环境，业务恢复重点是持久化和补偿，而不是在云函数里运行进程守护器。

- 任务采用租约和到期重领，函数中断后由下一轮继续。
- 失败任务指数退避，达到阈值进入人工处理队列。
- 每日对账，主动补查长期支付未确认和退款处理中订单。
- 部署保留可回退版本。
- 故障期间优先保留查单和退款；暂停接收新订单。
- 每日生成加密备份，保留7个日备份和4个周备份，并同步至店主控制的电脑。
- 每月执行恢复演练，核对订单、退款和积分一致性。

备份也消耗资源，先测算存量大小；若固定预算无法容纳必要恢复能力，标记为上线阻碍，不省略备份。恢复目标暂定数据损失窗口不超过24小时、人工恢复目标4小时，须经演练验证。

### 5.4 测试与上线顺序

1. **接入验证**：核实实际套餐、商户绑定、手机号权限、通知模板和 HTTPS 回调入口。
2. **交易闭环**：真机完成预约、支付、核销、服务完成、积分入账、取消和真实退款。
3. **并发验证**：分别用10、30、100个同秒请求争抢同一时段；只允许一个有效占位，其余返回冲突或繁忙。
4. **一致性验证**：重复下单、双设备消费积分、重复回调、支付超时、迟到支付、退款重试均不重复记账。
5. **竞态验证**：取消与支付、核销与自动退款、排班修改与预约同时发生时结果合法。
6. **故障验证**：函数中断、微信接口超时、通知失败、任务重复执行、资源不足以及备份恢复。
7. **安全验证**：跨用户读取订单、技师访问他人日程、伪造角色与金额、恶意上传和接口刷取。
8. **界面验证**：至少覆盖一台 iPhone、一台 Android 和不同屏宽模拟器，检查大字体与安全区。
9. **经营验收**：用固定测试账本核对新老客、收款、退款、服务收入和积分报表。

100人测试用于验证正确性和测量承载能力，不等于19.9元套餐承诺支持100人同时成功预约。正式上线以实测、真实支付退款闭环、安全检查和恢复演练全部通过为条件。
