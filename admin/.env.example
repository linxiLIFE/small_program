# CloudBase 环境 ID（非秘密）；前端通过 Web SDK 登录并调用 api 云函数。
VITE_CLOUDBASE_ENV_ID=cloud1-d9g5pfect2ece00fa

# 仅本地 UI 开发时可显式打开；生产构建保持 false 或不设置。
VITE_ADMIN_DEMO=false

# 腾讯位置选点 Web Key（仅允许你的管理后台域名使用，不是服务端密钥）。
VITE_TENCENT_MAP_KEY=
