# 微信支付接入清单

当前代码已接入支付下单、查单、关单、退款和回调处理，但真实交易仍受资质、公钥 ID 和真机验收条件控制；支付密钥不写入仓库。按下面顺序补齐。

## 需要在服务端填写的变量

| 变量 | 作用 | 是否允许进小程序/浏览器 |
| --- | --- | --- |
| `WX_APPID` | 小程序 AppID | 否，支付服务端使用 |
| `WX_MCH_ID` | 微信支付商户号 | 否 |
| `WX_MCH_SERIAL_NO` | 商户证书序列号 | 否 |
| `WX_API_V3_KEY` | API v3 对称密钥 | 否 |
| `WX_PRIVATE_KEY` | 商户 API 证书私钥，可用 `\\n` 表示换行 | 否 |
| `WX_NOTIFY_URL` | `payment-callback` 的公网 HTTPS 地址 | 否，服务端配置 |
| `WX_PLATFORM_PUBLIC_KEY_PEM` | 微信支付公钥，用于应答和通知验签 | 否 |
| `WX_PLATFORM_SERIAL_NO` | 微信支付公钥 ID（`Wechatpay-Serial`） | 否 |
| `WX_PLATFORM_CERT_PEM` | 微信支付平台证书，用于通知验签；与公钥二选一 | 否 |
| `WX_PLATFORM_VERIFIERS_JSON` | 公钥轮换列表，可并存多组 `serialNo` 与公钥/证书 PEM | 否 |
| `WX_NOTIFY_MAX_SKEW_SECONDS` | 通知时间戳最大偏差，默认 300 秒 | 否 |
| `CONTACT_ENCRYPTION_KEY` | 32 字节联系方式 AES-256-GCM 密钥，支持 64 位 hex 或 base64 | 否 |
| `QUOTE_SIGNING_SECRET` | 报价令牌签名密钥，必须至少 32 字节且无公开默认值 | 否 |

当前 `payment-callback` 已创建公网 HTTPS 访问路径，`WX_NOTIFY_URL` 已写入 `api`、`jobs`、`admin-api` 和 `payment-callback` 四个云函数的服务端环境变量：

```text
https://cloud1-d9g5pfect2ece00fa.service.tcloudbase.com/payment-callback
```

商户号 `1750539004`、小程序 AppID、商户证书序列号、API v3 密钥、商户私钥、公钥 PEM 和公钥 ID 已写入上述云函数的服务端环境变量，未写入仓库或小程序。公钥 PEM 已校验为 2048 位；当前服务端配置完整，可以进入真机支付验收。

2026-09-13 的状态机加固已在本地完成但尚未部署：退款意图先落库、通知去重与金额核对、迟到支付补退、`CLOSED` 新单号重试、`ABNORMAL` 人工处置及终态防回退均已进入代码。部署前必须先执行已有库索引迁移并同步共享模块。

代码只会在配置完整时调用 JSAPI 下单。配置不完整时订单仍可保留，页面显示“支付待配置”，不会伪造成功。

## 资质和配置步骤

1. 完成小程序主体认证，确认个体工商户主体可以申请小程序支付权限。
2. 申请普通商户号，确认商户号与当前 AppID 绑定。
3. 创建 API 证书，记录商户证书序列号和私钥；私钥只进入 CloudBase 服务端受控配置。
4. 设置 API v3 密钥并保存到密码管理器，再写入云函数环境变量。
5. 部署 `payment-callback`，配置公网 HTTPS，确认微信侧回调地址可访问且不会改写原始请求体。
6. 在商户平台“账户中心 → API 安全 → 微信支付公钥”申请并下载 `pub_key.pem`，记录公钥 ID；将其配置为 `WX_PLATFORM_PUBLIC_KEY_PEM` 和 `WX_PLATFORM_SERIAL_NO`。也可以改用平台证书，但两者二选一。
7. 用真机完成支付、服务端查单、支付取消、5 分钟关单、迟到支付自动退款、用户取消退款和退款成功通知。

## 不能用来判断成功的信号

- 小程序前端 `wx.requestPayment` 的 `success` 只说明调起流程返回，不等于服务端已确认到账。
- 微信退款接口受理成功只代表申请已受理，不等于用户已经到账。
- 只能依据支付回调或服务端主动查单更新支付终态，并根据退款回调或退款查单更新退款终态。

## 配置后验证记录

| 项目 | 结果 | 时间 | 备注 |
| --- | --- | --- | --- |
| 个体工商户主体认证 | 待验证 | — | 由店主补充 |
| 商户号与 AppID 绑定 | 待验证 | — | 由店主补充；下单前仍需确认 |
| 商户凭证写入服务端 | 已完成 | 2026-09-13 | 商户号 `1750539004`；不记录密钥内容 |
| 支付回调 HTTPS 入口 | 已完成 | 2026-09-13 | 入口可达；空报文按预期返回失败，不代表真实通知验签通过 |
| 微信支付公钥 PEM | 已取得并注入 | 2026-09-13 | `pub_key.pem` 已校验为 2048 位公钥；已写入四个云函数服务端环境 |
| 微信支付公钥 ID | 已完成 | 2026-09-13 | 已写入四个云函数；不能用商户证书序列号代替 |
| API v3 JSAPI 下单 | 待验证 | — | 真机测试 |
| 支付回调验签解密 | 待验证 | — | 记录 requestId |
| 主动查单和关单 | 待验证 | — | 覆盖超时和迟到支付 |
| 用户取消整单退款 | 待验证 | — | 覆盖退款回调 |
| 未到店自动退款 | 待验证 | — | 覆盖任务重试 |
