# 当前项目状态

> 这份文档是项目继续开发时的上下文记录。内容以实际操作和测试结果为准。

## 2026-09-22 审阅问题与目录卡片布局修复（云函数与后台已部署；小程序未上传）

- 管理后台小项目卡片的显示序号改为叠在封面上，不再占网格列；卡片宽度按边框盒计算，避免序号和拖动把手挤乱图片、标题及状态。款式网格同一行拖动按左右半区判断，跨行拖动按上下半区判断。
- 目录排序统一把 0、缺失或无效序号排在有效序号之后，并在后台响应中保留创建时间作旧记录的稳定回退；款式目录按大项、小项目、款式的人工顺序展示，不再被人气排序遮盖。编辑或拖动一个目录组时，服务端事务会压紧并整理该组的旧序号；未对生产目录执行迁移。
- 小程序只采用仍存在的本地图片缓存文件；发现路径失效时仅清除缓存索引，不删除本地文件，并继续显示网络图、尝试恢复临时 URL。卡片、首页横幅、详情、预约、订单及头像图片失败时会先回退网络地址，再显示占位图。
- 管理后台不再依赖 `createImageBitmap`；上传压缩和裁切统一用 `Image` 与 Canvas 处理，兼容缺少该 API 的浏览器。云函数临时图片 URL 对未解析文件重试一次。
- 发布门槛中的 `npm run prepare:functions`、相关 JavaScript `node --check`、共享模块 `cmp`、`git diff --check` 和管理后台 `vue-tsc`/Vite 生产构建均通过；Vite 仍提示 JS 分包超过 500 kB。按本轮工作约束未运行项目测试。
- 2026-09-22 已将 `api`、`admin-api` 单独部署到 `cloud1-d9g5pfect2ece00fa`，云端函数列表状态均为 `Deployment completed`；`jobs`、`payment-callback` 未更新，未部署 `seed`。两个入口的只读 `getHome` 和 `listServices` 调用成功，大项顺序为美甲 1、脚部美甲 2、睫毛 3、纹绣 4；服务列表各返回 51 项，抽查款式列表成功。未执行目录写入、数据库迁移或订单操作。
- 管理后台通过 `tcb hosting deploy admin/dist cloud-admin --safe --verify --json -e cloud1-d9g5pfect2ece00fa` 发布 17 个文件，`verified: true`，备份位于 `.cloudbase-backup/1790012231094/`。线上 `/cloud-admin/index.html` 返回 HTTP 200；`index-YFYe_0Bu.js`（SHA-256 `fc25df8b7a76936e94f6ceba86cb0344f3d384ac634425ca22d527c9bf01bc67`）与 `index-DztgM25-.css`（SHA-256 `5db2e7f26a86172ba0c563d06ecdbdc5d22cab6fdbd9c74fb8a7a794e6b1a390`）均与本地产物一致。
- 未上传小程序体验版，因此小程序本地图片缓存和顾客端图片回退尚未生效；后台排序布局尚未用真实店主会话现场操作验收。

## 2026-09-22 目录顺序编辑与图片缓存提速（后台与 api/admin-api 已部署；小程序未上传）

- 大项、小项目和款式按各自同级范围保存连续的唯一 `sort` 序号。管理后台卡片显示当前序号，长按排序把手可拖动；编辑窗口可输入序号，保存时在事务中移动条目并调整受影响的同级序号。新增条目排到同级末尾，归档删除后压紧序号。
- 顾客端大项、小项目和款式列表按已保存的目录顺序读取；首页精选继续使用精选列表自己的 `featuredSort`。
- 管理后台新上传图片优先转为 WebP，款式/项目图最长边 1024px，横幅最长边 1280px；不支持 WebP Canvas 的浏览器改用 JPEG。历史云图片不自动重写。
- 小程序 API 响应仍保留短时数据缓存；此外对带 CloudBase 文件 ID 的图片，首次网络显示后低优先级下载并保存最多 40 张、累计不超过 12 MiB 的本地副本，后续响应按文件 ID 替换为本地路径。缓存达到上限后停止新增缓存，不主动删除本地文件。
- 发布前已执行 `npm run prepare:functions`、`npm --prefix admin run build -- --emptyOutDir=false`、相关 JavaScript `node --check`、目录模块 `cmp` 和 `git diff --check`；后台构建通过，Vite 提示主 JS 分包超过 500 kB。按本轮工作约束未运行项目测试。
- 2026-09-22 将 `api`、`admin-api` 发布到 `cloud1-d9g5pfect2ece00fa`，函数列表反查均为 `Deployment completed`。`jobs`、`payment-callback` 未更新；生产函数清单未包含 `seed`。线上只读调用 `api/getHome` 返回 `InvokeResult=0`，大项顺序为美甲 1、脚部美甲 2、睫毛 3、纹绣 4；没有执行目录写入、数据库迁移或订单操作。
- 管理后台已通过 `tcb hosting deploy admin/dist cloud-admin --safe --verify --json` 发布 12 个文件，`verified: true`，安全备份位于 `.cloudbase-backup/1790010025601/`。线上 `/cloud-admin/index.html` 返回 HTTP 200；入口引用 `index-Ro2PMzaX.js`（SHA-256 `03223fa5308bb1ef61149f55af9018a1d41d932cf0c716db5cc876e9e3e2efbb`）和 `index-CKlYa1M_.css`（SHA-256 `7b1890e99cbb720d94c98dd42c7fc1cee431ce9a9f6b31c08c1782a62b33ab9f`），均与本地产物一致。
- 未上传小程序体验版；因此 `utils/image-cache.js` 的顾客端本地图片缓存仍需随小程序上传后才会生效。尚未使用真实店主会话操作拖动/保存排序，后台交互和小程序真机图片速度仍待验收。

## 2026-09-17 脚部美甲目录收口与按数量加脚甲片（服务端与后台已发布；小程序未上传）

- 脚部美甲客户端只保留 9 个可单独预约的小项目：本甲纯色 40/60/80/120、本甲简约款式 60、本甲简约建构款式 80、本甲建构轻奢款式 120、10 根脚甲片 40/80。脚部卸本甲、卸脚甲片、脚部 V 建构、脚部塑形建构和单片脚甲片均强制不可单独预约。
- 顾客选择脚部“本甲”小项目后，预约页只询问是否需要加脚甲片，不再显示卸甲或建构选择；选择需要后用 1–10 的加减控件选数量。每个脚甲片 ¥5，不增加服务时长，小计即时展示。10 根脚甲片小项目不出现该询问。
- `footTipCount` 已进入时段请求、服务端价格计算、报价签名、幂等哈希、下单复核和 `addonSnapshots`；服务端限制 0–10 个，单价固定 500 分、时长固定 0。手部和脚部的预约加项逻辑彼此独立。
- `npm run prepare:functions` 和 `npm test` 全通过（资金/并发不变量 49 条、产品功能回归 12 条），管理后台 `vue-tsc`/Vite 构建通过；微信开发者工具普通编译成功且问题面板为 0。
- `api`、`jobs`、`admin-api`、`payment-callback` 已部署到 `cloud1-d9g5pfect2ece00fa`，四个函数反查均为 `Deployment completed`，部署清单不含 `seed`。线上 `api/getSettings` 返回 `InvokeResult=0`；`jobs` 无写入冒烟返回 `processed: 0, failed: 0, repaired: 0, cleaned: 0`。
- 线上 `listServices(foot-nail)` 已实测只返回上述 9 个小项目；微信开发者工具已走通脚部本甲预约页，现场只出现“是否需要加脚甲片”，两个选择框及 1–10 数量控件均保持在卡片边界内，“不加时”角标已移除，问题面板为 0。未执行数据库迁移或修改生产目录数据。
- 管理后台已通过 `tcb hosting deploy admin/dist cloud-admin --safe --verify --json` 发布，9 个文件全部上传且 `verified: true`，安全备份位于 `.cloudbase-backup/1789626957391/`。公网入口引用 `index-wI-zjwDL.js` / `index-BvOL_zoy.css`，远端 SHA-256 与本地一致。本轮未上传小程序体验版，因此新的加脚甲片数量控件仍需上传后做体验版/真机验收。

## 2026-09-17 手部美甲卸甲计价按项目档位区分（服务端已发布；小程序未上传）

- 手部美甲按主项目价格判断卸甲费用：主项目价格大于 ¥30 时，卸本甲、卸甲片、卸超厚本甲建构三种方式均免费；价格等于 ¥30 时仅卸本甲免费，另外两种各 ¥20；价格低于 ¥30 时三种方式均按配置收费（当前 V 建构 ¥15 对应 10/20/20 元）。
- 预约上下文和补拉叠加项接口都会携带当前主项目；服务端在报价、创建订单前复核和订单快照阶段使用同一价格，避免只改展示。
- `npm run prepare:functions` 已同步共享函数，`npm test` 全通过，共享文件 `cmp`、JavaScript 语法和 `git diff --check` 通过；修正版 `api`、`jobs`、`admin-api`、`payment-callback` 已再次发布并反查为 `Deployment completed`。线上实测 V 建构为 10/20/20 元、¥30 档为 0/20/20 元、¥50 档为 0/0/0 元；`jobs` 冒烟为 `processed: 0, failed: 0, repaired: 0, cleaned: 0`。未上传小程序、未修改生产数据。

## 2026-09-17 技师加项、后台首屏、邀请卡、付款订阅授权与新图标（云函数与管理后台已发布；小程序未上传）

- 小程序技师页和管理后台“技师管理 → 个人工作台”的当天预约卡片现在都会展示订单快照中的卸甲、建构加项，包含加项名称、增加时长和价格（免费加项明确显示“免费”）。服务端 `publicOrder()` 原本已返回 `addons`，本轮仅补齐两个界面的格式化与卡片展示，未修改历史订单数据。
- 管理后台启动不再先串行整理全部目录、设置和排班数据：验证会话后直接进入经营概览；目录与设置改为进入相关页面时按需加载并在本次会话中缓存，完整排班只在进入“技师排班”时加载。首屏提示改为“正在进入门店后台”，页面自身的数据继续由对应模块加载。
- 积分页的邀请模块改为常驻卡片：直接显示“我的邀请码”、复制操作和“输入邀请码”绑定区；移除折叠入口、分享按钮和页面分享处理。原有“仅新用户可绑定、禁止自绑、只能绑定一次”的服务端规则不变。
- “我的”页已移除“微信预约提醒”模块。有现金微信支付的订单改为在确认拿到可用支付参数后、调起 `wx.requestPayment` 之前请求预约成功/到店/退款三类订阅；新订单首次支付和订单详情“继续支付”两条路径都会重新请求本次授权。
- 订阅状态从单个 `acceptedAt/usedAt` 布尔门闩改为 `acceptedCount/usedCount` 可累积次数；发送前通过事务领取一次额度和通知记录租约，发送失败会事务归还额度。这同时支持每次付款重新授权和同一顾客存在多个待发提醒，并兼容旧的时间戳记录。
- Safari/PWA 图标替换为单条开放式花瓣/光环与暖金光点，不再使用四条丝带、四重旋合或数字 4 意象。新高清源图保存在 `assets/branding/safari-app-icon-source.png`，`scripts/build-admin-icons.swift` 可重现带奶油色底的 32/180/192/512 PNG 发布资产。

### 本轮验证与发布边界

- `npm run prepare:functions` 已同步共享函数；`npm test` 全通过，其中包含 47 条资金/并发不变量、9 条产品功能回归、11 个小程序页面与 16 个 WXML 验证，以及层级、样式流、后台和身份隔离回归。`vue-tsc` / Vite 生产构建、相关 JavaScript 语法和 `git diff --check` 通过。
- 本地演示后台已实际打开经营概览和“技师管理 → 林老师 → 个人工作台”：经营概览无需等待目录、设置和排班汇总即可出现；预约卡现场显示“卸甲 / 卸本甲 / +15分钟 / 免费”和“建构 / V 建构 / +20分钟 / ¥150.00”。
- `api`、`jobs`、`admin-api`、`payment-callback` 已部署到 `cloud1-d9g5pfect2ece00fa`，函数列表反查均为 `Deployment completed`；部署清单不含 `seed`。线上 `api/getSettings` 无写入调用返回 `InvokeResult=0` 和 settings v19。
- 管理后台已通过 `tcb hosting deploy admin/dist cloud-admin --safe --verify --json` 发布，9 个文件全部上传且 `verified: true`，安全备份位于 `.cloudbase-backup/1789621006442/`。公网入口已引用 `index-uG33-_7c.js` / `index-C91V_Q5t.css`；JS、CSS、`apple-touch-icon.png` 和 `manifest.webmanifest` 的远端 SHA-256 均与本地产物一致。
- 32px favicon 与 512px PWA 图标已实际渲染检查，小尺寸下轮廓仍可识别。当前运行的微信开发者工具没有可用窗口，本轮无法完成三个小程序页面的现场视觉点击验收。
- 本轮未执行数据库迁移、未部署 `seed`、未上传小程序体验版，未发起支付、退款、新订单或真实订阅消息。每次支付的真机订阅弹窗与消息送达仍需在小程序上传后用测试订单验收；管理后台带真实店主会话的加项展示仍需打开线上订单现场确认。

## 2026-09-16 核销后自动完成、卸甲/建构叠加、图片容错与 Safari 主屏幕图标（服务端、数据库和后台已发布；小程序未上传）

- 预约叠加项后续修正：MySQL 迁移写入的布尔值可能表现为 `1/0`，服务端已兼容读取，避免预约页只显示“不需要”；普通美甲/脚部美甲继续分别选择卸甲和建构款式，小项目名称含“建构”时只询问卸甲，服务端同时拒绝重复叠加建构。
- 预约页组合接口若未带回完整叠加项，现在会自动调用独立接口重取，兼容/演示数据也已补齐，不再直接弹出“选项未加载完整”。开发者工具实际走通：普通“本甲纯色｜30元色板”显示 3 种卸甲和 3 种建构；“本甲建构纯色”只显示 3 种卸甲，没有重复建构问题。
- 卸甲与建构现在同时保留为可单独预约的小项目：美甲 6 个、脚部美甲 4 个，每个项目只有 1 个同名款式；单独预约卸甲不再重复询问附加项，单独预约建构只询问是否卸甲。卸甲单独预约均收费：手部“卸本甲” ¥10、“卸超厚本甲建构” ¥20、“卸甲片” ¥20，脚部“卸脚部本甲” ¥10、“卸脚甲片” ¥20；作为美甲或建构的卸甲加项时，只有“卸本甲”/“卸脚部本甲”免费，其余卸甲按项目价收费，所有选项均叠加服务时长。线上目录已迁移并核对为美甲 19 个小项目/19 款式、脚部美甲 13 个小项目/13 款式。
- 预约页移除重复说明文案并重做卸甲/建构选项卡；选项网格、按钮和长名称均限制在卡片宽度内，解决按钮边框向外突出的布局问题。开发者工具已实际确认普通项目显示卸甲与建构双组，V 建构只显示卸甲组，底部导航图标正常加载。

- 到店核销时新增 `SERVICE_COMPLETE` 定时任务，预约结束后会把 `ARRIVED` 或 `IN_SERVICE` 幂等转为 `COMPLETED` 并只发放一次积分；`jobs` 同时分页修复缺失完成任务的到店/服务中订单。工作人员仍可在服务结束后手动完成。发布后执行 `jobs` 冒烟为 `processed: 0, failed: 0, repaired: 0`，只读查询确认线上当前没有滞留在 `ARRIVED` / `IN_SERVICE` 的订单。
- 美甲和脚部美甲预约页新增必须先回答的卸甲/建构选择：均可明确选择“不需要”；卸本甲加项免费，其余卸甲和建构均叠加价格，所有加项叠加时长。服务端将选择绑定进签名报价、幂等请求、排班占用、订单金额及订单快照，并在下单事务内复核项目仍有效及有效加项价格；订单详情、顾客订单列表与后台订单表展示所选叠加项。
- 线上目录迁移保留原手部卸甲/建构人工时长，并写入“是否加项免费”标记：仅手部“卸本甲”和脚部“卸脚部本甲”为免费卸甲加项，其他卸甲与建构在两个入口均按项目价收费。管理后台的小项目编辑器可继续把美甲/脚部项目设置为普通项目、卸甲选项或建构选项，并要求所有可单独预约的小项目价格大于 0。
- 图片链路在原有上传裁剪压缩、云文件临时 URL 缓存、小程序 WebP/懒加载基础上，补充后台异步懒加载与失败占位，以及预约、订单详情、订单列表、款式详情的失败降级，单张失效图片不再留下破图。
- 管理后台新增 Safari/PWA 元数据、Manifest、32/180/192/512 图标。当前主屏幕图标采用无文字的四条扁平丝带旋合图形，高清源图位于 `assets/branding/safari-app-icon-source.png`，发布图标位于 `admin/public/`；后续更换源图不需要改业务逻辑。

### 本轮验证与发布边界

- `npm test` 全通过：47 条资金/退款/并发不变量、8 条产品功能回归、小程序 11 个页面与 16 个 WXML 校验，以及款式流、后台、层级和身份隔离回归；管理端 `vue-tsc` / Vite 生产构建、共享函数同步、相关 JavaScript 语法和 `git diff --check` 通过。
- `api`、`jobs`、`admin-api`、`payment-callback` 已部署到 `cloud1-d9g5pfect2ece00fa`，函数列表反查均为 `Deployment completed`；线上 `api/getSettings` 返回 `InvokeResult=0` 和 settings v19。
- 管理后台通过 `tcb hosting deploy admin/dist cloud-admin --safe --verify --json` 发布，9 个文件全部上传且 `verified: true`，备份位于 `.cloudbase-backup/1789534198344/`。线上入口引用 `index-ChBCLmrt.js` / `index-DlLGgbuG.css`，JS、CSS、`apple-touch-icon.png`、`manifest.webmanifest` 的远端 SHA-256 均与本地一致。
- 双入口目录与唯一款式规则已再次发布 `api`、`admin-api`；管理后台最新构建以安全校验模式上传 9 个文件并通过 `verified: true`，备份位于 `.cloudbase-backup/1789554575632/`。本轮仍未上传小程序体验版。
- 卸甲价格迁移已更新线上 5 条服务记录并反查成功；生产 `listBookingAddons` 返回手部卸本甲 0、卸甲片 2000、卸超厚本甲建构 2000，脚部卸本甲 0、卸脚甲片 2000。`api`、`admin-api` 已重新部署，管理后台 9 个文件安全发布并通过 `verified: true`，最新备份位于 `.cloudbase-backup/1789555906723/`。微信开发者工具实际确认普通美甲和 V 建构两条预约路径均显示“卸本甲免费”，其余卸甲显示 ¥20 及叠加时长。
- 本轮未上传小程序体验版，因此卸甲/建构顾客界面、订单叠加项展示与新增图片失败占位尚未进入体验版；未发起支付、退款或新订单。Safari 真机“添加到主屏幕”的最终图标缓存与启动展示仍需在 iPhone 上验收。

## 2026-09-15 项目与款式三级删除（云函数与管理后台已发布；小程序未上传）

- 管理后台的大项、小项目、款式卡片均增加删除入口及影响范围确认；删除采用目录归档，大项删除连带归档子小项目和款式，小项目删除连带归档子款式。目录图片文件不清理，已创建的顾客订单及其项目、款式、时间、价格快照不修改。
- 顾客目录不再展示归档条目，也不能用旧项目或款式创建新预约；订单提交时在事务内再次检查大项、小项目及款式，防止删除与旧报价并发时产生新订单。
- 本地 `npm test`、管理后台生产构建、共享函数同步比较、JavaScript 语法及 `git diff --check` 通过；三级删除回归覆盖子项归档、订单快照保留和已删除条目拒绝再编辑。本地演示后台已点击新建大项→确认删除→列表移除，并检查小项目删除弹窗。
- 按本功能范围将 `api`、`admin-api` 部署到 `cloud1-d9g5pfect2ece00fa`，函数列表反查均为 `Deployment completed`；两个函数 `getSettings` 无写入调用均成功，返回 settings v19。使用无效大项编号进行路由冒烟：`api` 返回 `INVALID_ID`，`admin-api` 返回 `UNAUTHENTICATED`，均未触发目录写入。
- 管理后台以 `tcb hosting deploy admin/dist cloud-admin --safe --verify --json` 发布，4/4 文件上传且 `verified: true`，安全备份位于 `.cloudbase-backup/1789477914431/`；线上入口引用 `index-DCbIgxK6.js` / `index-DlLGgbuG.css`，远端与本地 SHA-256 分别一致为 `737bbc601399fcb5efeb195c584cf4d6df6dc2a92044606dc34810659ebd8ae8` / `89e2f3cea5223eddcdfcd514eab171ea52e4ed7a463f1583941f3484f03f7dd6`。未操作真实目录或真实订单，未部署 `jobs`、`payment-callback`、`seed`，未上传小程序体验版；带真实店主会话的线上删除点击和真机预约保留尚未验收。

## 2026-09-15 微信订阅消息真实模板配置（服务端已发布；小程序未上传）

- 根据公众平台模板详情配置并发布 settings v19：预约成功 `thing1 / date2 / thing19`，到店提醒 `thing2 / time1 / thing7`，核销成功 `thing1 / time5`，退款成功 `thing1 / amount6 / thing3`。核销模板不再发送旧技师字段，退款模板不再发送旧状态字段；退款成功通知覆盖用户取消、后台退款和未到店退款成功回调。
- `api`、`jobs`、`admin-api`、`payment-callback` 已重新部署；线上 `api/getSettings` 无交易冒烟返回 `version: 19`，四组模板 ID 和字段与公众平台详情一致。管理后台已用 `--safe --verify` 发布。
- 小程序首页不再展示首屏订阅提醒卡片，订阅授权保留在预约提交/付款流程；本轮未上传小程序体验版，真机授权、预约/退款/核销实际送达仍待测试订单验收。

### 本轮验证

- `npm test` 全通过（订阅模板数据、日期/时间类型和退款对账回归均通过）；管理端 `vue-tsc` / Vite 构建、共享云函数同步、JavaScript 语法和 `git diff --check` 通过。
- 本次按改动范围重新部署 `api`、`admin-api`，未部署 `seed`；线上函数列表反查两者均为 `Deployment completed`，两个函数的 `getSettings` 无数据写入冒烟均返回 `InvokeResult: 0`。
- 管理后台已用 `tcb hosting deploy admin/dist cloud-admin --safe --verify --json` 发布，7 个文件上传成功并通过远端校验，备份位于 `.cloudbase-backup/1789467597088/`；线上入口引用的 JS/CSS 与本地产物哈希一致。

## 2026-09-15 自动取消订单隐藏与历史全退款订单对账（线上已修复）

- `ord_mu260lc5_2488c8abb6` 的订单为 `CANCELLED`、支付记录为 `CLOSED`，从未支付。后台预约列表原来只排除 `CANCELLED_BY_USER` 的未付款单，现将 `CANCELLED` 自动关单也纳入排除条件；只读查询按新条件反查该订单返回 0 行。订单、支付原始记录未删除。
- `ord_mu1e7bf9_bc59fecf8d` 实付 4000 分，已有两笔 `SUCCESS` 退款 1000 分和 3000 分，合计正好 4000 分；后来因为订单累计字段仍为 3000 分，后台误显示可退 1000 分并建立 `_3` 退款尝试。该尝试被微信以“订单已全额退款”拒绝，状态为 `CONFIG_OR_DATA_ERROR`、未产生成功退款，补偿任务为 `DONE`。在精确前置条件和成功退款合计校验下修正订单主记录为 `CANCELLED_NO_SHOW / SUCCESS`、`refundedFen=4000`、`refundId` 指向已成功的 `_2`；保留“未到店已取消”业务状态及 `_3` 审计记录，没有重新退款。
- 后台列表现在按所有 `SUCCESS` 退款记录汇总已退金额；管理员再次申请退款时会在订单事务锁内复核成功退款流水，超额或已全退订单直接拒绝，退款到账更新也会对历史成功流水复核。避免其他多笔历史退款订单再次误显示可退余额。

### 验证与边界

- `npm test` 全通过，包含 46 条资金/并发不变量和 6 条产品功能回归；共享云函数已同步，相关语法和 `git diff --check` 通过。
- `api`、`jobs`、`admin-api`、`payment-callback` 四个生产函数已重新部署并反查 `Deployment completed`；线上 `api/getSettings` 无交易冒烟返回 `InvokeResult=0`。未部署 `seed`，没有重新发布管理后台静态文件或上传小程序。
- 线上只读回查：自动取消单符合新排除条件；全退款单为 `CANCELLED_NO_SHOW / SUCCESS`、累计 4000 分，退款表两笔 `SUCCESS` 合计 4000 分。没有发起新退款、数据库迁移或删除订单/退款记录；带真实店主会话的后台页面点击路径尚未现场复核。

## 2026-09-15 订单、部分退款、精选、邀请与后台体验调整（云函数与管理后台已发布；小程序未上传）

- 后台订单查询支持用完整手机号匹配加密联系方式，并在有权限的订单页、技师工作台直接显示可拨打手机号；隐藏用户自行取消且从未支付的订单，同时保留顾客已软删除的已支付订单供后台继续退款和审计。订单列表新增下单时间，预约时间统一显示为起止时段，各订单状态使用独立颜色。
- 管理员退款支持选择退回剩余全部或输入部分金额；服务端校验单笔金额不超过剩余可退金额，累计记录已退款金额，并在部分退款到账后继续保留退款入口。未到店已取消订单同样可退款；旧退款成功回调在后续退款期间晚到时不会重复累计。
- 管理后台新增“精选管理”，可集中添加已上架款式、取消精选和调整首页顺序；项目与款式中的原精选开关保留。经营概览改为净收款趋势，移除订单状态分布并替换为收退款概览，自定义日期按钮重新设计。
- 网页核销支持浏览器实时相机扫码；没有 `BarcodeDetector` 时使用 `jsQR` 逐帧识别，并增加手机拍照后识别的 Safari 兼容入口。小程序订单详情直接展示预约二维码，同时在详情内增加订单号、下单时间和支付时间。
- 小程序邀请入口收进积分页，不再占用“我的”大推荐卡；邀请码长期有效、同一邀请人可邀请多人，但被邀请人必须从未下过单且只能绑定一次。
- 技师个人工作台的当天预约补充款式照片、项目、顾客完整手机号、金额、订单号、状态色和预约起止时段。管理后台移除“微信支付接入”选项卡及可见设置版本号；各保存操作只显示成功结果或具体失败提示。

### 本轮验证与边界

- 根目录 `npm test` 全部通过，包括 46 条资金/退款/并发不变量、5 条二维码与部分退款功能测试、后台精选排序回归、小程序 11 个页面与 16 个 WXML 校验；管理端 `vue-tsc` / Vite 生产构建、相关 JavaScript `node --check`、共享云函数同步比较和 `git diff --check` 通过。
- 已在本地演示后台实际打开经营概览、预约订单、部分退款弹窗和精选管理，确认净收款、自定义日期、完整手机号、时间段、下单时间、扫码/拍照入口、状态色和剩余可退金额可见；浏览器控制台无错误。
- 已执行 `npm run prepare:functions`，并将生产清单中的 `api`、`jobs`、`admin-api`、`payment-callback` 部署到 `cloud1-d9g5pfect2ece00fa`；四个函数均反查为 `Deployment completed`，未部署 `seed`。线上 `api/getSettings` 返回 `InvokeResult=0`。
- 已使用 `tcb hosting deploy admin/dist cloud-admin --safe --verify --json` 发布管理后台，4 个文件全部上传并通过远端校验，备份位于 `.cloudbase-backup/1789445367258/`。线上引用 `index-liAU0web.js` 与 `index-Cz6qk-qV.css`，两者 SHA-256 均与本地产物一致。
- 本轮未执行数据库迁移、未上传小程序体验版、未操作真实订单或真实退款。iPhone Safari 相机权限与真实照片识别、小程序真机二维码展示以及部分退款到账/重复退款仍需用测试订单验收。

## 2026-09-14 预约二维码、未到店规则、订阅提醒、邀请与后台分析（服务端、数据库和后台已发布；小程序未上传）

- 顾客订单详情新增签名预约二维码；店主/员工可核销全店订单，技师账号只允许核销分配给自己的预约。小程序技师工作台和网页后台均增加扫码入口，并保留二维码篡改、过期、订单状态与到店时间窗口校验。
- 新订单保存取消/未到店规则快照：默认开始前 120 分钟内不能自行取消，开始后 15 分钟未核销自动按 30 元结算；积分抵扣优先承担违约金，剩余现金退款，订单总额低于违约金时不退款。数值可在管理后台修改，预约页同步展示。
- 接入预约成功、到店提醒、核销成功和未到店退款四类微信订阅消息；模板 ID 与关键词字段由后台配置。浏览器安全策略阻止读取当前 Chrome 公众平台页面，因此真实模板值尚未填入，真机授权与送达尚未验收。
- 新增邀请码与一次性绑定，默认邀请双方各得 10 积分并写入双方积分流水；奖励数值可在后台调整。
- 后台订单支持日期、顾客/订单号、状态、技师、小项目、款式、金额/时间排序与分页；经营分析支持 7/30/90/365 天和最多 366 天自定义范围，新增退款、取消、完成率、未到店率、大项/状态分布与预约时段热力。
- 同步修复完整审阅中的支付准备租约、任务 CAS、顾客跨技师重叠预约、CI 依赖安装、15 分钟排班、报价完整绑定、资料并发更新、首笔积分锁、目录排序、数值排序、订单分页、退款/支付文案、未来预约下的技师停用、回调并发 ACK、锁顺序、幂等过期清理、目录静默截断、接口限流与时间轴高频更新等问题。
- 订单 `ord_mu0ohiy3_5ef9ce7758` 的退款回调曾因错误读取不存在的 `resource.tag` 而卡在 `PROCESSING`；同时发现 `jobs` 与 `admin-api` 没有公网出口，无法主动访问微信退款查单。现已修复回调解密、为两个函数开启出站公网访问，并由正式补偿任务向微信查单确认成功。线上订单为 `REFUNDED / SUCCESS`，退款记录为 `SUCCESS / SUCCESS`，补偿任务为 `DONE`，管理后台显示“已退款 / 退款已到账”。

### 本轮验证与发布边界

- `npm test` 包含 45 条资金/退款/并发不变量及 4 条新功能回归；微信 API v3 回调解密、二维码防篡改、积分优先扣费和低金额不退款测试通过。管理端 `vue-tsc` / Vite 构建、JavaScript 语法与 `git diff --check` 通过。
- 已在线执行 `docs/mysql-product-features-migration.sql`，反查确认 3 个生成列、4 个目标索引和 `rate_limits` 表存在；邀请码唯一约束应用前未发现重复值。
- `api`、`admin-api`、`jobs`、`payment-callback` 已部署且均为 `Deployment completed`，四个函数均配置独立 `CHECKIN_SIGNING_SECRET`；`jobs` 与 `admin-api` 公网出口为 `ENABLE`，固定 EIP 保持关闭。`jobs` 最终冒烟 `failed: 0`，支付回调入口对无效 GET 返回预期 HTTP 500。
- 管理后台已用 `--safe --verify` 发布，线上 `index-CMPNrexL.js` 与本地产物 SHA-256 均为 `494a41b93ac5c9d4770d1063928eb20e93a1c0fa2b2d57f4c1495367641b8d71`。规则已发布为 v11：开始前 120 分钟禁止自行取消、开始后 15 分钟未核销自动结算、违约金 30 元、邀请双方各 10 积分。
- 按用户要求未上传小程序体验版。微信订阅消息开关已启用，但四个模板 ID 仍为空；浏览器安全策略不允许读取公众平台已登录页面，因此模板配置与真机送达仍待补充。
- `npm audit --omit=dev` 仍报告 CloudBase/微信 SDK 依赖链的 13 个问题；自动修复涉及 SDK 主版本变更或降级，未直接套用，需单独兼容升级。

## 2026-09-14 最新安全复审后续修复（云函数、管理后台与统计迁移已更新；小程序未上传）

- 退款状态统一先去掉微信事件的 `REFUND.` 前缀，再映射本地状态；`REFUND.CLOSED` 进入 `RETRY_REQUIRED`，`REFUND.ABNORMAL` / `USER_ACCOUNT_ABNORMAL` 进入 `MANUAL_ACTION`。`markRefundAbnormal()` 优先使用已分类的本地状态，不再被原始 provider code 覆盖回 `PROCESSING`。
- 创建订单在锁定最新 `technician_days` 行后重新调用完整时段校验，同时复核休息日、最新班次、休息区间和预约占用；管理员刚缩短当天排班时，旧报价不能继续落单。
- 订单详情从 `paymentConfirming=1` 进入时会继续主动查单，页面重新显示时也会续查；轮询仍未收敛时提供“重新确认支付结果”按钮，并用单实例锁避免重复轮询。全积分订单显示“积分支付完成，无需微信支付”，退回后显示“积分已退回”。
- `jobs` 分别按最早到期时间读取待处理任务和过期租约，再公平交替领取，持续有新任务时过期 worker 也不会饥饿；支付/退款补偿扫描使用持久 ID 游标分页，每轮推进最多 100 条并在末页回绕，不再永久只扫描前 100 条。
- 员工身份查询统一为 UID 优先、未命中后回退 openid；`staffSession()` 与权限校验使用同一策略，兼容历史 openid 绑定记录。
- 新库 schema 与统计迁移增加 `(user_id, completed_at)` 复合索引，降低长期新客/复购查询成本；新增 GitHub Actions CI，覆盖共享模块同步、根目录测试、管理端构建、JavaScript 语法和 diff 校验。

### 本轮验证与边界

- `npm test` 全部通过：45 条资金/退款/并发不变量，以及新增任务公平性、补偿游标、支付确认恢复、全积分文案、排班复核和 UID/openid 回退测试。
- `npm run prepare:functions` 已同步 `api` 共享模块到 `jobs`、`payment-callback`、`admin-api`、`seed`，逐文件比较一致；全部 JavaScript `node --check`、管理端 `vue-tsc` / Vite 构建和 `git diff --check` 通过。
- 已将 `api`、`jobs`、`admin-api`、`payment-callback` 四个生产云函数部署到 `cloud1-d9g5pfect2ece00fa`，四个状态均为 `Deployment completed`，部署清单未包含 `seed`。线上 `api/getSettings` 返回 `InvokeResult=0` 且 `minAdvanceMinutes=15`；线上 `jobs` 冒烟返回 `processed: 0, failed: 0, repaired: 1`。
- 已用 `--safe --verify` 将管理后台重新发布到 `/cloud-admin/`，4 个文件全部上传并通过远端校验，备份保存在 `.cloudbase-backup/1789370433233/`；线上 `index-oQkdSq4Z.js` 与本地产物 SHA-256 均为 `f9d0ba8a07bee0081b58d8862e8e626dfc8e99e51ac21b05d1aded61ad9df7fb`。
- 已在线执行 `docs/mysql-analytics-index-migration.sql`。反查确认 `orders` 的 `work_id`、`paid_at`、`completed_at`、`paid_fen`，`refunds` 的 `success_at`、`amount_fen` 共 6 个生成列，以及 5 个统计索引全部存在；`idx_orders_user_completed` 列顺序为 `(user_id, completed_at)`。
- 支付回调公网入口对无效空 GET 返回预期的 HTTP 500。小程序体验版仍未上传，因此订单详情的支付确认恢复和全积分文案等顾客端修改还不会在体验版生效；GitHub Actions 需提交并推送后才会成为仓库检查，真实 MySQL 多连接竞态与真实微信支付/退款仍属于上线验收边界。

## 2026-09-14 项目安全审阅后续修复（历史发布记录；最新线上状态见上节）

- 已移除 `adminBootstrapStatus` / `adminBootstrapOwner` 线上 action 和后台按钮。首个 OWNER 只能由受控终端使用预先指定 CloudBase UID 执行 `npm run admin:bootstrap-owner`；普通已登录用户不再能抢占店主权限。
- `jobs` 领取写入随机 `claimToken` 和租约，完成、延期、失败重试均以 `jobId + RUNNING + claimToken` 条件更新；旧 worker 无权覆盖新 worker。任务按 `businessId` 分组、最多 4 组并发，同一订单保持串行。
- 退款外部提交增加 `submissionId`、generation 和 lease 的事务抢占；只有当前 submission owner 调用微信退款并回写结果，其他执行器只查单或等待。管理端退款按钮增加按订单点击锁。
- 预约事务先 `INSERT IGNORE` 建立 `technician_days` 主键行，再 `SELECT ... FOR UPDATE` 检查并写入占用；每周排班发布用稳定 guard 与预约事务串行，只在写事务中复核未来有效订单，不再锁历史订单和全部日期记录。
- 订单详情以 `paymentStatus/paidAt`、`completedAt`、`refundStatus` 分别展示支付、履约和退款事实；支付按钮增加锁，支付成功后按 0/1/2/4/8 秒查单，确认期间禁止重复支付和取消；首屏加载失败可重新加载。预约无有效报价时金额显示 `--`。
- 服务完成必须达到订单 `endAt`；金额统一要求 JavaScript safe integer，项目价格上限为 1000 万分（10 万元）；数据库连接必须显式设置 `DB_USER`，不再默认 root。
- 首页热门款式改为 MySQL 按有效状态聚合，后台经营概览改为 SQL 汇总/趋势/排行，不再把全部 orders/refunds 拉入 Node.js。新库 schema 已加入统计生成列和索引；已有库迁移文件为 `docs/mysql-analytics-index-migration.sql`。当次发布尚未执行迁移，现已在上节记录的最新发布中完成。
- 按用户要求，未修改预约时间轴 `touchmove` 高频 `setData` 逻辑。

### 本轮验证与边界

- 根目录 `npm test`、相关 JavaScript `node --check`、管理端 `vue-tsc` / Vite 构建已通过；新增安全加固结构回归、safe integer、价格上限和“完成时间不得早于 endAt”断言。
- 已运行 `npm run prepare:functions` 同步共享模块，并将 `api`、`jobs`、`admin-api`、`payment-callback` 部署到 `cloud1-d9g5pfect2ece00fa`；四个函数均为 `Deployment completed`，部署清单未包含 `seed`。线上 `api/getSettings` 调用成功；旧 `adminBootstrapStatus` action 返回 `UNKNOWN_ACTION`；`jobs` 冒烟返回 `processed: 0, failed: 0, repaired: 0`。
- 已用 `--safe --verify` 将最新管理后台发布到 `/cloud-admin/`，4 个文件全部上传且远端验证成功；公网 `index.html` 已加载 `index-oQkdSq4Z.js`，远端脚本与本地产物 SHA-256 一致，且不再包含 `adminBootstrapStatus` / `adminBootstrapOwner`。
- 当次发布未执行线上 MySQL 统计索引迁移、未上传小程序体验版；统计迁移现已在上节记录的最新发布中完成，小程序界面修复仍需上传后才能在用户端生效。
- 真实扣款、支付成功通知、取消竞态、退款提交和退款到账仍需商户解除“当前商户存在异常”后完成真机 E2E；代码或模拟测试不能代替真实资金验收。

## 2026-09-14 上一轮完整审计修复（代码与云端已更新）

- 取消与预支付并发改为“先在本地事务内取消并标记 `CLOSE_PENDING`，再由独立支付对账任务查单/关单”；每次预支付使用随机尝试令牌，只有仍属于当前订单的尝试才能提交支付参数。迟到成功会进入持久退款意图，不会被后续重复回调取消。
- 下单幂等键现在绑定服务、款式、技师、日期、开始时间、积分和报价版本；同一键携带不同业务参数会返回 `IDEMPOTENCY_CONFLICT`，网络重试同一请求仍复用原订单。
- 退款错误按微信返回码分流：系统错误、频率限制和网络不确定结果继续查单重试；余额不足进入 `WAITING_FUNDS`；账户异常进入人工处理；参数、权限、签名或商户配置错误进入 `CONFIG_OR_DATA_ERROR`。后两类不会无限自动请求，店主修复后可在订单页用原退款单号手动重试；关闭退款使用新退款单号重发。
- 支付/退款通知使用数据库事务、事件哈希 ID、随机抢占令牌和处理租约；完成状态不可被旧处理覆盖。通知继续校验时间戳、验签序列号、付款人、币种、支付金额、退款金额和原支付单号。
- 服务状态增加门店时间约束：仅可在预约前 60 分钟至到店宽限期内确认到店，开始服务不能早于预约前 15 分钟；当时完成服务只限制到预约开始时间，后续修复已收紧为不得早于 `endAt`。重复“确认到店”不会覆盖原 `arrivedAt`。
- 未核销预约不再自动退款，超时后进入 `NO_SHOW_REVIEW`，由店主确认实际未到店后退款，或由工作人员确认到店继续服务，避免“已实际服务但忘记点到店”造成自动退钱。
- 顾客只能软删除退款无需处理或退款成功的取消订单；退款提交中、余额不足、配置异常和人工处理中订单保持可见。后台已补齐这些状态和“重新退款”入口。
- 管理端日期筛选和技师本人筛选先下推 SQL 再执行 `LIMIT`；订单新增 `booking_date` 生成列，设置版本使用数值生成列 `version_num`，避免 v10 被排到 v9 前面。事务继续对死锁/锁等待最多重试 3 次。
- 已完成服务以 `completedAt` 作为履约事实，后续退款仍保留完成次数、顾客数和复购统计；收款、退款和净收款仍分别按支付/退款到账事实统计。
- 小程序和网页管理端统一用 `Asia/Shanghai` 显示门店日期与时间，避免设备在其他时区时预约显示偏移。

### 本轮线上变更与验证

- 已在当前 CloudBase MySQL 执行 `docs/mysql-finance-index-migration.sql`，并由 `information_schema` 确认订单用户/状态索引、技师/日期/开始时间索引、支付商户单号与交易号唯一约束、退款单号唯一约束、任务到期索引和设置数值版本索引均已生效；执行前检查未发现资金编号重复数据。
- 已删除远端旧 `seed` 云函数；2026-09-14 再次读取函数列表只存在 `api`、`jobs`、`admin-api`、`payment-callback`，四个函数均为 `Deployment completed`。本地保留受双重开关保护的初始化源码，且不在生产部署清单内。
- 已重新部署上述四个函数。线上调用 `api/getSettings` 成功，当前设置包含 `noShowPolicy=MANUAL_REVIEW`；线上调用 `jobs` 返回 `processed: 0, failed: 0, repaired: 0`。
- 本地根目录全部测试通过，其中资金状态不变量为 39 条；全部 JavaScript 语法检查、管理后台 `vue-tsc`/Vite 构建和 `git diff --check` 均通过。
- 代码与静态/线上冒烟已经完成；真实扣款、微信支付成功通知、真实退款到账通知仍必须由具备正常支付权限的真实微信账号和商户完成端到端验收，不能用静态测试或云函数调用替代。当前已知手机端曾在 `wx.requestPayment` 被微信提示商户异常，需先由商户平台解除权限/风控问题后再验收真实资金链路。

## 2026-09-13 支付、取消、退款与并发状态机加固（历史记录；最新状态见上节）

- 取消待付款订单前会主动查询微信状态；`NOTPAY` 先关单，`SUCCESS` 转入退款，`USERPAYING`/未知状态会持久化并阻止取消，`ORDER_NOT_EXIST` 明确按未创建支付单收口。
- JSAPI 下单带服务端 `paymentDeadline` 对应的 `time_expire`；预支付增加 `PREPAY_SUBMITTING` 中间态，避免“取消与创建预支付单”并发时把支付参数交给已取消订单。
- 迟到支付和重复成功通知都按“当前订单状态 + 当前退款状态”判断，且在支付成功事务中保证退款意图和补偿任务存在；支付回调不再同步等待微信退款 API。
- 退款改为先持久化 `INIT/SUBMITTING` 意图再请求微信；`SUCCESS` 不可回退，`CLOSED` 转为 `RETRY_REQUIRED` 并由店主使用新退款单号重试，`ABNORMAL` 转为 `MANUAL_ACTION`。
- 管理员退款先将订单切到 `CANCEL_PENDING_REFUND` 并释放占用；退款中/已退款订单不能继续到店、开始或完成服务，退款成功后统一进入 `REFUNDED`，完成订单的奖励积分会冲回。
- 全积分零元订单在创建事务内直接消费积分，不创建零元微信退款；取消或未核销时按幂等账本退还积分。
- 微信通知增加 5 分钟时间戳新鲜度校验、事件 ID 去重记录、付款人/币种校验，以及退款单号、原支付单号、退款金额和原金额核对；支持通过 `WX_PLATFORM_VERIFIERS_JSON` 配置多把验签公钥轮换。
- 每分钟任务会重新扫描到期但非终态的支付和未完成退款，补建丢失/失败的查单任务；即使通知最终丢失，也会由主动查单恢复本地状态。
- MySQL 查询把可表达的条件、排序和限制下推到 SQL，事务死锁/锁等待最多重试 3 次；新 schema 为订单、支付、退款和任务添加生成列索引及资金编号唯一约束，已有库迁移见 `docs/mysql-finance-index-migration.sql`。
- 生产部署清单不再包含 `seed`；种子函数自身还要求 `SEED_ENABLED=true` 和至少 32 字节的 `SEED_ADMIN_SECRET`。当前云端旧 `seed` 函数不会因本地配置自动消失，本轮未做远端删除。

### 本轮验证边界

- 新增 26 条支付、退款、回调、防回退、死锁识别和 SQL 索引约束回归断言；根目录 `npm test` 全部通过。
- 管理后台 `vue-tsc` 与 Vite 构建通过；相关 JavaScript `node --check` 和 `git diff --check` 通过后再交付。
- 2026-09-13 20:46 已将 `api`、`jobs`、`payment-callback`、`admin-api` 四个目标函数部署到 `cloud1-d9g5pfect2ece00fa`；四个函数均为 `Deployment completed`，未部署也未删除远端旧 `seed` 函数。
- 线上只读 `api/getHome` 调用成功，`InvokeResult=0`；`payment-callback` 公网路由仍可达，空 GET 返回 500，符合回调入口拒绝无效报文的预期；`jobs` 的 `shiguang-jobs-every-minute` 触发器保持启用。
- 本轮未执行 MySQL 索引/唯一约束迁移、管理后台发布、小程序体验版上传或真实金额支付/退款；云函数部署成功不等于支付、退款和并发资金链路已经通过生产验收。

## 2026-09-13 已取消订单支持删除（云函数已部署；体验版未上传）

- 已取消、用户取消和未到店已取消的订单仍可从记录页进入详情；详情底部显示“删除订单”，点击后先二次确认。
- 删除采用软删除：只从顾客订单记录中隐藏，服务端校验订单归属且只允许取消状态执行；支付、退款、服务凭证和后台审计数据继续保留。
- 新增 `deleteOrder` 云函数接口，并同步到全部共享模块；列表接口过滤已删除订单，后台订单查询仍保留原记录。

### 本轮验证边界

- `npm test`、全部相关 JavaScript `node --check` 和 `git diff --check` 已通过。
- `tcb fn deploy --all --force --install-dependency true` 已成功部署 `api`、`jobs`、`seed`、`payment-callback`、`admin-api` 五个云函数；云端函数列表均为 `Deployment completed`，`jobs` 定时触发器保持启用。
- 线上只读 `api/getHome` 调用成功；未在真实订单上执行删除，也没有上传小程序体验版。

## 2026-09-13 待付款倒计时与查看详情按钮优化（云函数已部署；体验版未上传）

- 待付款订单在记录页和订单详情页显示剩余支付时间，按订单服务端截止时间实时倒计时；倒计时结束后自动刷新支付状态并展示取消结果。
- “查看详情”使用明确的固定小尺寸，覆盖原生按钮默认宽度；金额、倒计时和操作按钮在卡片底部保持对齐。
- 保留服务端 5 分钟支付占位、每分钟任务关单和支付状态复核；新增到期后查询到未支付时立即关单的服务端兜底，未修改订单表结构。

### 本轮验证边界

- `node --check`、`npm test`、`npm run prepare:functions` 和 `git diff --check` 已通过；微信开发者工具模拟器已复核记录页和订单详情页的倒计时文案及按钮样式。
- `tcb fn deploy --all --force --install-dependency true` 已成功部署 `api`、`jobs`、`seed`、`payment-callback`、`admin-api` 五个云函数；`jobs` 的 `shiguang-jobs-every-minute` 触发器仍为启用状态。
- 本轮没有上传小程序体验版，也没有执行真机支付或生产验收；云函数自动关单已通过线上任务调用和只读订单状态核对。

## 2026-09-13 订单预约时间段与记录页细节优化（本地已验证；体验版待上传）

- 订单记录页、订单详情页和工作人员工作台统一按“开始时间—结束时间”显示预约时段；优先使用订单 `endAt`，历史数据缺少结束时间时按服务时长补齐。
- 记录卡片的金额标签和按钮增加不可换行及固定对齐处理，避免“待支付”在窄屏中被拆成竖排；预约时间单独成行，长时间段不再挤压其他信息。
- “查看详情”调整为更小的固定宽度轻量按钮，和金额信息保持同一行的右侧对齐。
- 使用统一的小程序时间段格式化函数，未修改云函数和订单数据结构；订单时间展示仍以服务端保存的具体起止时间为准。

### 本轮验证边界

- `node --check`、`npm test` 和 `git diff --check` 已通过；微信开发者工具模拟器已重新编译并复核订单记录页和详情页，均显示完整预约时间段及缩小后的“查看详情”按钮。
- 本次没有重新上传体验版，也没有执行真机支付或生产验收。

## 2026-09-13 顾客端订单页视觉重构（本地已验证；体验版待上传）

- 订单记录页改为固定的信息层级：款式图片、预约状态、预约时间、服务技师、服务时长、金额和“查看详情”；移除订单内部 ID、时间字符图标和箭头符号。
- 订单状态筛选收敛为全部、待付款、待到店、进行中、已完成和已取消；“进行中”继续合并已到店/服务中，“已取消”覆盖三种取消状态。
- 订单详情页拆分为预约状态、服务内容、费用明细和订单进度，增加款式图片与服务信息网格；待付款金额显示为“待支付金额”，常规预约显示“实付金额”。
- 隐藏 `NOT_REQUIRED` 退款状态，只展示退款待处理、处理中、已到账、已关闭和异常等用户可理解的状态；服务项目与款式同名时不重复展示。
- 空订单状态改为 CSS 图形，不再直接外显装饰字符；详情底部操作按钮统一使用居中布局。

### 本轮验证边界

- `node --check pages/orders/index.js pages/order-detail/index.js`、`npm test` 和 `git diff --check` 已通过；测试仍保留既有图片解析失败诊断输出，不代表线上图片全部可用。
- 微信开发者工具本地模拟器已实际打开预约记录页和订单详情页，确认新层级、金额文案、退款状态过滤和按钮文字可见；尚未重新上传体验版，也未做真机验收。

## 2026-09-13 微信支付凭证接入与回调入口（已部署；待真机支付闭环）

- 已核验 `/Users/linxi/Documents/1750539004_20260913_cert`：商户 API 证书主题为 `1750539004`，商户证书和私钥公钥匹配，API v3 密钥文件为 32 字节；具体密钥内容未写入仓库、文档或回复。
- 已将小程序 AppID、商户号、商户证书序列号、API v3 密钥、商户私钥和通知地址写入 CloudBase `api`、`jobs`、`admin-api`、`payment-callback` 四个云函数的服务端环境变量，并保留原有数据库/VPC配置；四个函数均为 Active。
- 已从凭证目录取得并校验 `pub_key.pem` 为 2048 位微信支付公钥，并将公钥 PEM 和 `PUB_KEY_ID_0117505390042026091300291796003800` 注入四个云函数服务端环境；本地配置检查已无缺失项，使用公钥验签模式。
- 已创建并实测回调地址 `https://cloud1-d9g5pfect2ece00fa.service.tcloudbase.com/payment-callback` 可达；空报文返回 500 是预期的业务拒绝，不代表真实微信通知已验签。
- 支付代码现在支持微信支付公钥或平台证书验签，并校验 `Wechatpay-Serial`；HTTP 回调入口保留云函数事件入口，原始请求体不改写。
- 已修复退款申请重试的幂等恢复、退款查单、退款异常/关闭通知以及未到店退款失败补偿任务，并将修复部署到 `api`、`jobs`、`admin-api`、`payment-callback`；仍未用真实用户发起支付或退款。
- 使用商户凭证调用微信 `/v3/certificates` 做无交易验证时，微信返回 `RESOURCE_NOT_EXISTS`，说明该商户没有可用的平台证书；当前已改用微信支付公钥，不依赖该接口获取平台证书。无交易 API 冒烟已确认商户签名请求到达微信，CloudBase `getHome` 仍成功。
- 尚未用真实用户发起支付，未执行扣款、退款或改动任何订单资金状态；下一步是体验版/真机小额支付、支付回调/查单和真实退款验收。
- 已修复小程序报价请求遗漏 `date` 导致服务端报价校验失败、前端将空报价兜底显示为 `¥0.00` 的问题；现在报价请求携带所选日期。线上 `getService` 已读回 `svc-nail-french` 的 `priceFen=29900`，开发者工具重新编译后现场打开预约页已显示“项目金额 ¥299.00”“微信支付 ¥299.00”，支付按钮已启用；回归断言、`validate-miniprogram.js` 和 `git diff --check` 通过。体验版/真机支付仍待重新上传和验收。
- 真机调起支付排查：最新一次订单实际金额为 `¥0.01`，本地 `payments` 记录已到 `PREPAY_CREATED`，且记录中的 AppID 与商户号和当前配置一致；这证明服务端已成功调用 `/v3/pay/transactions/jsapi` 获取预支付标识，异常发生在手机端 `wx.requestPayment` 阶段，当前没有本地支付成功记录。微信开发者工具当前真机调试控制台只保留了无效返回路由等无关错误；商户侧需优先检查小程序支付权限/绑定及风控限制，并改用 `¥0.10–¥1.00` 的真机测试金额，不能把 `¥0.01` 测试结果当作商户支付能力结论。

## 2026-09-12 开户网络修复、时间轴预约与顾客端视觉更新、款式详情多技师选择（已部署；详情页与无动画修订待上传）

- 技师个人账户开通故障已定位：`api` 云函数原来保留 VPC 但未开启公网访问，调用 CloudBase 账号服务时会被网络策略拦截，管理端再统一显示为“服务暂时繁忙”。当前已保留 VPC、开启 `api` 公网访问并重新部署；账号服务异常会返回 `ACCOUNT_PROVIDER_UNAVAILABLE` / `ACCOUNT_PROVIDER_CONFIG` 等可定位错误和请求标识。只修复并验证链路，没有代替店主创建技师账号。
- 预约服务端新增 `timeline`：按营业班次、休息、已占用、提前预约限制和不可用区间拆段；小程序按 15 分钟时间点选择，白色为可预约、纹理区间为占用/休息/不可预约，拖动任一端时另一端按服务总时长实时跟随，首尾时间点可触达，松手后才吸附到可选起点并刷新报价；没有可预约时段时直接显示“当天没有可预约时段”。
- 管理端小项目和款式图片均使用 1:1 裁切并保存；小程序项目/款式卡片统一方形。预约无款式时保留抽象艺术图和“选款”按钮，首页“联系门店”按钮收窄，“我的”改为待付款、待到店、进行中、已完成四类订单入口；进行中筛选同时覆盖“已到店”和“服务中”。顾客端只保留页面标题、业务名称、价格、时长、日期、状态和功能按钮文字，删除欢迎语、宣传语、解释性提示、引导小句及英文。
- 款式详情页不再只显示款式记录关联的默认技师；页面会列出当前小项目下全部可接待技师，顾客可在详情页点选技师，点击预约时将选择带入预约页。当前本地开发工具已看到林老师和周老师并实测可切换；这一版详情页修改尚未重新上传体验版。
- 用户隔离回归测试通过：用户主键、订单和积分账户按微信 `openid` 隔离；手机号只作为加密保存的预约联系方式，不作为主键，也不会因为相同手机号自动合并账号。

### 本轮验证边界

- 本地 `npm test`、`npm --prefix admin run build` 和 `git diff --check` 通过。测试仍会输出既有的 `media resolution failed { code: undefined }` 图片解析诊断缺口，不能视为完全无警告。
- 线上 `api` 部署状态为 Active，确认公网访问已开启且 VPC 保留；下载线上代码确认包含时间轴、技师账号错误映射和手机号隔离逻辑。线上 `getAvailableSlots` 已返回 `timeline`，同时包含不可预约/可预约区段和可选起点。
- 管理后台已重新构建并安全发布，远端静态文件与本地产物一致；开发者工具中已实际看到“项目与款式”、方形款式图库、预约时间轴和“我的”四类订单入口。
- 微信开发者工具已上传体验版 `1.0.2`，明确显示“代码上传成功”；本次详情页多技师选择和取消时间条动画的修订尚未重新上传，未提交审核或正式发布。真机拖动时间轴、详情页多技师选择、手机号授权额度、真实技师首次登录和支付仍待验收。

## 2026-09-11 技师能力、预约体验与门店设置更新（已部署）

- 技师能力改为选择大项 `categoryIds`，同一技师可同时接待美甲、眉毛等多个大项；旧 `skills` 字段只保留读取兼容，保存时不再作为主数据。
- 服务只保留服务总时长，排班可用时段、报价和订单快照均按 `durationMinutes` 计算，已移除整理时间/缓冲时长概念。
- 管理后台和小程序默认店名统一为“四个小姐姐的店”。已有云端自定义设置不会被代码默认值强行覆盖，需要在后台保存一次门店设置后才会更新云端当前版本。
- 小程序首页和“联系客服”均调用 `wx.makePhoneCall`；用户资料页新增用户名编辑与 `updateProfile` 服务端接口。
- 后台门店设置增加腾讯位置选点回填和浏览器当前位置回填。配置 `VITE_TENCENT_MAP_KEY` 后可在后台内嵌选点；未配置时打开腾讯坐标页，复制经纬度后填入表单。保存前仍需人工确认坐标确实属于门店。
- 小程序预约空状态改为图片引导；预约记录改为更大的卡片；全局页面和预约/订单相关页面收窄横向布局、移除负边距与可横向拖动溢出，并统一字体、居中和按钮布局。

### 本轮验证边界

- 本地 `npm test`、管理端 `npm --prefix admin run build`、变更 JavaScript 语法检查和 `git diff --check` 通过。
- 已执行 `npm run prepare:functions` 同步共享模块，并用 `tcb fn deploy --all --force --install-dependency true` 成功更新 `api`、`jobs`、`seed`、`admin-api`、`payment-callback` 五个云函数；云端服务端配置保持不变。
- 管理端 `vue-tsc`/Vite 构建通过，`tcb hosting deploy admin/dist cloud-admin --safe --verify` 上传 4 个文件并远端校验成功；线上首页标题已确认是“四个小姐姐的店”。本次没有使用远端清理参数。
- 线上 `tcb fn invoke api` 调用 `getHome` 成功，返回店名“四个小姐姐的店”、5 个大项和当前宣传图；预约、用户名编辑、真实拨号、地图选点仍需在真实登录会话和微信真机上复测。小程序体验版尚未重新上传。

## 2026-09-11 CloudBase 环境迁移（已部署）

- 已将项目切换到新的 CloudBase 环境 `cloud1-d9g5pfect2ece00fa`（控制台名称“cloud1 微信体验版”），仓库中的 `cloudbaserc.json`、`cloud-config.js` 和管理端默认环境均已同步。
- 新环境已开通原生 Serverless MySQL 8.0：实例 `cynosdbmysql-ins-qk0hxufp`，集群 `cynosdbmysql-d56zmje2`，数据库名为环境 ID，内网地址 `172.17.0.11:3306`，VPC/子网为 `vpc-92zwp758` / `subnet-6sf7e7m9`。
- `docs/mysql-schema.sql` 已在新库执行并通过 `information_schema` 确认 19 张业务表；已创建 `shiguang_app` 业务账号并授予 19 张表的 `SELECT`、`INSERT`、`UPDATE`、`DELETE` 权限。密码和联系方式加密密钥只保存在云函数服务端环境变量中。
- `api`、`jobs`、`seed`、`admin-api`、`payment-callback` 均已部署并为 Active；`jobs` 的 `shiguang-jobs-every-minute` 每分钟触发器已创建。五个函数均已配置新 MySQL 内网连接、报价签名和联系方式加密服务端变量。
- 已调用新环境 `seed` 写入 5 个大类、52 个小项目、3 位技师、48 个统一占位图款式和设置；其中 4 个项目带有“可叠加”标记，`api.getHome`、`api.listServices` 和指定新项目的款式/技师读取均返回新库数据，`jobs` 返回 `processed: 0, failed: 0`。
- 新增目录中的 48 个小项目时长均已随机分配为 60、90 或 120 分钟，图片统一使用同一张占位图；数据库核对确认 48 个新增项目无孤立款式，4 个叠加项带有 `isAddon=true`、`bookableStandalone=false`。当前预约页仍沿用“一单一个小项目”的既有流程，叠加项的预约页勾选、合并报价和订单快照尚未开发。
- 管理后台已用新环境 ID 构建并发布到 [新管理后台](https://cloud1-d9g5pfect2ece00fa-1485433457.tcloudbaseapp.com/cloud-admin/index.html)，HTTP 返回 200，远端静态文件与本地产物一致。新 CloudBase 测试域名首次打开时会显示平台风险提醒，需要用户自行确认访问。
- 初始化脚本已改为显式指定 CloudBase MySQL 连接器和数据库名，后续使用 `npm run db:schema` 时不会误走 PostgreSQL 默认路径。

### 本次迁移仍未完成

- 当时新环境的 CloudBase 身份认证用户和 `staff_accounts` 店主授权尚未用真实账号完成，原计划使用网页一次性按钮；该方案已在 2026-09-14 后续审阅中废止，当前必须使用受控 UID 初始化脚本。
- 当时 `payment-callback` 仅部署为事件型云函数且尚未创建公网 HTTPS 访问路径；2026-09-13 已创建 `/payment-callback` 网关地址并写入服务端通知配置。微信支付公钥/平台证书、真实支付/退款和回调验签仍按清单待配置。
- 小程序体验版需要在微信开发者工具重新选择新环境并重新编译/上传；本轮只验证了新环境云函数和管理后台，没有把小程序正式发布到线上。

## 2026-09-11 首页、选款与后台稳定性更新（迁移前记录）

- 首页改为横幅宣传图轮播；网页后台新增“首页宣传”，支持本地上传、预览、前后移动与保存，最多 20 张。移除首页“选款预约”按钮。测试宣传图验证后已从当时配置移出，历史版本和云文件保留；9 月 11 日最后读回的当前配置含 1 张宣传图，原样保留。
- 选款先选择具体大项，再选择某个小项目并进入独立款式页；款式按累计有效预约数排列，并显示爱心和次数。有效预约包括已预约、已到店、服务中、已完成，排除未付款、取消、已成功退款和归档订单。首页精选仍由店主勾选。
- 我的页面移除工作台入口；管理和技师个人排班继续使用网页。底部四个图标重新生成透明 PNG，重新打开开发者工具项目后已在模拟器确认正常显示。
- 后台纠正大类总数，移除分类、项目、款式、技师的手动展示顺序输入及精选顺序输入；保留旧数据字段以兼容历史记录。增加侧栏与内容间距，统一图库、编辑器、宣传图和分析页面布局。
- 款式图片保存持久云文件标识，读取时换取显示地址；新增图片失败占位和预览恢复处理。重新进入管理页会刷新目录及图片地址，临时图片地址解析失败不会导致整个接口失败。下架款式允许其所属项目已停用，新增记录重试复用同一 ID，保存和审计一起提交。
- 门店地址与腾讯地图经纬度均可在后台修改。配置准确坐标后，小程序使用 `wx.openLocation` 打开位置；缺少坐标时复制地址。现有地址“哈尔滨市南岗区底下商店美甲店”无法唯一定位，尚未填写坐标；外部地图导航的真机体验仍待验证。
- 经营概览新增 7/30/90 天范围、收款趋势、净收款、退款、客单价、完成服务、到店顾客、新老客和款式/项目/技师预约排行。新客按历史首次完成服务日期判定，老客为此前已完成过服务的顾客；收款与退款按实际支付/到账日期统计，预约排行按本期创建的有效预约统计。空数据真实显示为 0，不填充演示交易。
- 设置发布改为事务内读取最新版本、校验并追加版本，同时提交审计；每周排班模板发布也合并最新设置，避免相互覆盖。预约/积分数值及地图坐标在服务端校验。小程序预约天数读取后台规则。
- 调整数据库空闲连接管理，仅对明确断连的只读查询重试。后台分批独立加载页面数据；小程序真实环境接口失败时展示错误和重试入口，不再回退为演示数据。详情页及预约时段加载补上错误处理。

### 2026-09-11 选款分步页面与宣传图裁切（已部署）

- 顾客端预约改为“大项 → 小项目 → 款式”：选款页只负责选择大项和小项目，点击小项目进入新增的 `pages/style-select/index`，再选择款式进入详情和预约；服务端报价与下单继续强制要求有效 `workId`，小项目的服务时长用于计算时段。
- 管理端项目目录同步显示大项、小项目、款式层级及小项目服务时长；移除本轮新增的英文标题和解释性提示，保留必要业务名称、数量、价格和时长。
- 首页宣传图在后台上传时居中裁切为 2:1，小程序横幅使用铺满裁切显示，避免两侧留白。
- 本轮已在微信开发者工具模拟器实际走通“选择大项 → 选择小项目 → 选择款式 → 款式详情 → 预约”，预约页读到已选款式；随后已部署 `api`、`admin-api` 和小程序体验版。

### 2026-09-11 加载性能与预约状态保留（已部署）

- 小程序读接口加入短时内存缓存和同参数请求去重；首页与选款页返回时保留已有内容，不再先清空页面；预约页改为一次读取预约上下文，再读取实时可用时段和报价。
- 目录云函数将大类、小项目和款式改为一次目录读取后复用，首页不再返回当前页面用不到的技师和小项目图片；新增只读取当前小项目款式的接口。为兼容尚未更新的云函数，小程序暂时保留旧接口回退路径。
- 图片列表增加懒加载和 WebP 请求，首张横幅保持立即加载；管理端宣传图继续居中裁切为 2:1，所有新上传图片压缩到最长边 1280 像素，降低后续图片体积。历史云图片不会被自动重写。
- 通过全局选款状态保存大项、小项目和款式；从款式详情返回不重新加载页面，从预约页点“更换”进入选款时保留当前款式标记，返回预约页时保留日期、时间和技师选择。
- 本地 `npm test`、管理端 `vue-tsc`/Vite 构建和开发者工具模拟器验证通过。2026-09-11 已将 `api`、`admin-api` 更新到 CloudBase；`tcb fn invoke` 验证 `getHome` 成功，函数耗时 26ms，`listServiceStyles` 返回指定小项目的款式。

### 本轮实际验证与发布

- `npm test` 全部通过，包含领域规则、11 个页面/16 个 WXML、分步选款流程、管理权限/CRUD/上传/排班，以及分析统计口径、配置校验、图片失败隔离、PNG 图标检查。后台 `vue-tsc` 与 Vite 构建通过；构建保留旧产物，没有直接删除文件。
- 真实店主网页完成本地宣传图上传和保存；线上 `getHome` 返回宣传图云文件标识及显示地址，模拟器确认横幅展示。测试后曾恢复空宣传配置；9 月 11 日用户现有配置为 1 张宣传图，最终保存保留该配置，没有替换素材。
- 非法开放天数 0 保存时返回明确校验错误，设置版本未增加；恢复有效值后保存成功。9 月 11 日再次原样保存当前门店/预约设置，页面确认 v10 发布成功，地址、电话、预约和积分规则保持原值。
- 9 月 11 日真实店主网页再次编辑现有“不知道”款式并原样保存，返回“已保存”；图库显示 3 个大类、5 个款式。已确认远程照片能加载；该现有款式原始上传文件本身为 72×72 图标，并非照片，保留原内容。
- 模拟器已确认宣传区域、具体大类/小项目筛选、爱心次数和底部四个图标；后台真实登录会话已确认目录、图片和 7/30/90 天分析界面可加载。新老客及有交易数值的统计由本地样本测试覆盖，线上当前没有真实交易数据。
- `api` 与 `admin-api` 最新代码已更新到原 CloudBase 环境，保留服务端配置；管理后台静态页面已发布到 `/cloud-admin/`，远端一致性校验通过。此次没有重新部署 `jobs` 或 `payment-callback`。
- 小程序 1.0.4 首次上传后，模拟器发现预约页缺少 `getBookingContext` 导出；已补兼容回退，重新编译并上传 1.0.5，开发者工具显示“代码上传成功”。重新走到预约页后，已选款式、技师和预约日期区域正常显示，未再出现该报错；尚未提交审核或正式发布。

### 尚未证实的故障原因

- 用户报告的历史“服务器繁忙”和全站加载失败，本轮未复现。设置保存后已多次调用首页、项目与设置接口成功；这些结果证明当前检查时链路可用，不证明历史故障根因已消除。
- 当前云函数没有开启历史日志服务，CLI 日志请求返回 `ResourceNotFound.TopicNotExist`，控制台说明开启前不会补采集。未擅自开启可能计费的日志服务。
- 检查时 MySQL 连接为 22、历史峰值 29、配置上限 1000，未发现连接数耗尽证据。已加入可定位的错误码和请求标识，后续若复现应据此定位，不能把猜测写成根因。

以下按日期保留开发历史；与本节冲突的早期状态以本节为准。

## 2026-09-09 网页管理重构

- 管理端保留网页形式；技师使用同一网页登录，TECHNICIAN 角色进入个人工作台。未在小程序新增个人排班页面。
- 项目与款式：新增/编辑大类、小项目、款式；图片使用本地选择、压缩和云存储上传，数据库保存 cloud 文件标识，读取时解析显示地址。管理员可切换图库、小项目、首页精选并调整精选排序。
- 技师管理：新增、编辑、停用、头像、可接待项目、查看排班；店主可开通用户名密码账号，服务端绑定技师身份。个人网页提供两周日历、班次、休息时段、休息日和本人预约，服务端限制只修改本人排班且不覆盖已有预约。
- 门店地址仍在网页“门店与预约设置”修改，小程序直接读取展示。保留用户当前门店设置，不重新初始化。
- 修复星期计算偏移；管理列表正确区分已上架/已停用；客户首页只显示管理员设为精选且所属项目、大类有效的款式。

### 本轮验证

- `npm test` 通过：领域规则、10 页/15 个 WXML、款式筛选与预约传递、分类/项目/款式/技师 CRUD、精选、上传拒绝非法格式、个人权限及预约冲突。
- `npm --prefix admin run build -- --emptyOutDir=false`、`git diff --check` 通过。
- 已用真实店主网页会话新增大类、技师、本地上传款式并保存；SQL 确认图片为云文件标识。验证记录已停用并归档保留，无删除。
- 店主打开技师个人工作台，保存当天休息成功，回读日历显示休息，未来两周工作日从 14 变为 13。
- 真实网页设为精选后，线上 `api.getHome` 仅返回所选奶油法式美甲款式，测试后恢复原精选配置。
- `api`、`admin-api` 最新代码已部署；管理网页静态托管已发布。
- 技师账号创建接口、账号绑定和自动角色切换已实现；尚未使用真实技师账号验证 CloudBase 创建用户权限及首次登录闭环，不能视作已验收。个人页面真实保存是在店主预览模式验证，技师越权由本地测试覆盖。
- 真机支付、支付回调及手机号授权资质仍保持待验收状态。

## 更新时间

- 2026-09-14

## 项目定位

当前项目已从 HelloWorld 骨架扩展为“四个小姐姐的店”单店美业预约首版：顾客端采用原生微信小程序，业务后端采用 CloudBase 云函数，另有 Vue 3 + TypeScript 管理后台。目标仍是“先自用、逐步扩展”，没有接入 Azure 服务器。

## 2026-09-09 图片选款与界面精简

- 顾客端移除装饰英文、营销语、辅助说明；保留项目名称、价格、时段、订单状态与操作按钮。首页移除最近作品，采用大图入口、分类入口、精选项目和直接显示的门店地址；底部四栏配套本地 PNG 图标。
- 选款页按“全部 / 大分类”与“全部款式 / 小项目”两层筛选，双列图片加名称；只展示仍有有效所属项目的款式。详情通过 switchTab 传递项目、款式、技师到预约页；预约标签没有选择时显示选款入口。
- 后台“项目与款式”新增款式创建、编辑、图片地址预览、关联项目、排序及上下架；仅店主可保存，服务端校验图片协议和所属项目，并从项目派生分类。当前图片入口为链接，不含本地文件上传。
- 订单存储经过服务端验证的款式快照，顾客订单详情、工作人员列表与后台订单展示款式名称。
- 后台“门店与预约设置 → 门店资料 → 地址”可修改地址；首页每次显示重新读取。已按用户原文保存“哈尔滨市南岗区底下商店美甲店”到新的设置版本 v2，保留 v1 与原预约、积分规则；数据库读回确认。
- api、admin-api 仅更新代码，保留现有云端配置；管理后台静态资源已上传并通过远端文件验证。小程序仍为本地开发版本，没有提交审核或发布正式版。
- 验证：npm test（业务规则、小程序静态检查、筛选及预约参数传递、店主权限及款式校验）；后台 vue-tsc 与 Vite 构建通过。开发者工具实际显示新版图片选款页、四栏图标与新地址，并已点击款式进入详情再进入预约，确认图片、名称、技师与可选时段加载；后台线上登录页已加载。后台登录后的真实款式保存、真实支付及新订单款式落库尚未完成实测。

## 已确认配置

| 项目 | 当前值 |
| --- | --- |
| 小程序 AppID（当前项目配置） | `wxa9eecac18eb834da` |
| CloudBase 环境名称 | `cloud1 微信体验版` |
| CloudBase Env ID | `cloud1-d9g5pfect2ece00fa` |
| 开发工具 | 微信开发者工具 Stable 2.02.2608070 |
| 当前页面 | `pages/index/index` |
| 云函数根目录 | `cloudfunctions/` |
| CloudBase 数据库 | 原生 Serverless MySQL 8.0（19 张业务表） |
| MySQL 实例 | `cynosdbmysql-ins-qk0hxufp` / `cynosdbmysql-d56zmje2` |
| 云函数网络 | `vpc-92zwp758` / `subnet-6sf7e7m9` |
| 管理后台静态地址 | `https://cloud1-d9g5pfect2ece00fa-1485433457.tcloudbaseapp.com/cloud-admin/index.html`（新环境已发布） |
| Web SDK 安全来源 | 新静态托管域名已发布；新环境真实账号登录和安全来源首次验收仍待完成 |

## 已完成

- 保留并验证过原有 `hello` 云函数链路。
- 顾客端：首页、服务分类、项目详情、作品详情、预约流程、订单列表/详情、积分明细和个人中心；管理入口在网页。
- 可复用组件：项目卡、作品卡、状态徽章、空状态、区块标题；统一奶油白/低饱和玫瑰视觉和安全区样式。
- 顾客端统一 API 封装；只有未配置云环境的本地演示模式使用样例数据，已配置环境的调用失败不会伪装成成功。
- `cloudfunctions/api`：目录、服务端报价、技师每日占用事务、订单幂等、积分冻结/消费/解冻/奖励/退款退回和奖励冲回欠额、身份和角色检查、管理操作和统一错误码。
- `cloudfunctions/jobs`：支付超时查单/关单、取消竞态对账、未到店人工复核和退款补偿；`payment-callback`：支付和退款 API v3 通知验签解密入口；`admin-api`：管理后台 HTTPS API 入口。
- `cloudfunctions/seed`：仅保留本地初始化源码，要求显式开关、管理员密钥和确认字符串；不在生产部署清单，远端函数已删除。
- 已切换到 CloudBase 原生 Serverless MySQL 8.0：`docs/mysql-schema.sql` 已执行，19 张表已创建；云函数通过参数化 SQL 和 JSON 业务载荷访问，预约冲突使用 MySQL 短事务。
- 已创建服务端业务账号并授予 19 张表的 `SELECT`、`INSERT`、`UPDATE`、`DELETE` 权限；数据库密码只保存在 CloudBase 云函数环境变量中，没有写入仓库。
- `api`、`jobs`、`admin-api`、`payment-callback` 已部署到当前环境并绑定 MySQL 所在 VPC；`jobs` 已创建每分钟定时触发器 `shiguang-jobs-every-minute`，生产环境不部署 `seed`。
- 已在线执行受控种子初始化：当前目录为 5 个大类、52 个小项目、3 位技师、48 个款式、1 条设置版本；`api.getHome`、`api.listServices` 和 `jobs` 调用均已返回成功。
- `admin/`：Vue 3 + TypeScript + Vite 管理后台，包含经营概览、订单、项目、技师、预约/积分规则和支付接入状态页面。
- 管理后台已接入 `@cloudbase/js-sdk`：用户名密码登录、本地持久会话、退出登录、真实 `api` 云函数调用；服务端使用 CloudBase Node SDK 读取当前 `uid/openId`，管理操作仍由 SQL `staff_accounts` 角色授权。
- 技师排班编辑器已接入真实管理 API：读取每周营业模板和指定日期的技师安排，支持班次、休息时间、请假/休息状态编辑；服务端写入 `settings_versions`、`schedule_templates` 和 `technician_days`，并在已有有效预约时拒绝冲突排班。
- 当时加入了网页首个店主初始化入口；2026-09-14 后续安全审阅确认存在抢占风险，现已从本地代码移除并改为受控运维脚本。
- 顾客端手机号绑定已接入真实 `getPhoneNumber` 回调：绑定按钮同时触发隐私授权，回调会区分用户取消、隐私指引未声明、手机号能力无权限和体验版额度耗尽等错误，不再把所有失败都提示为“您已取消登录”。
- 已在微信开发者工具重新编译并上传小程序 1.0.2，工具确认代码上传成功并覆盖当前体验版。
- 技术方案和微信支付待补清单已写入 `docs/`。

## 当前目录

```text
app.js / app.json / app.wxss
components/
pages/
utils/ / types/
cloudfunctions/api/
cloudfunctions/jobs/
cloudfunctions/payment-callback/
cloudfunctions/admin-api/
cloudfunctions/seed/
admin/
scripts/
docs/
```

## 已执行的本地验证

| 命令 | 结果 |
| --- | --- |
| `npm test` | 本地通过领域规则、46 条资金状态不变量、任务公平性、支付确认恢复、安全加固结构、11 个页面/16 个 WXML 资源及管理端回归校验 |
| `find . -name '*.js' ... node --check` | 通过 |
| `cd admin && npm run build` | 通过 `vue-tsc` 和 Vite 生产构建 |
| 微信开发者工具模拟器 | 首页编译通过，并切换到项目页看到演示项目、作品和底部导航 |
| 小程序手机号授权代码 | 本地 `node --check`、WXML 资源校验通过；微信开发者工具已上传 1.0.1；真实手机授权结果仍需在已配置隐私指引且具备手机号能力的体验版中验证 |
| 管理后台 | 最新版本已发布到 CloudBase `/cloud-admin/` 并通过远端校验；公网资源与本地产物哈希一致，首个店主按钮和公开初始化 action 已移除，首次 OWNER 改用受控脚本。免费套餐不支持新增本地安全域名 |
| CloudBase MySQL | 资金唯一约束、订单/任务/设置索引及统计生成列/索引已在此前发布中经 `information_schema` 确认；2026-09-15 本轮没有数据库迁移 |
| CloudBase 云函数 | 当前只部署 `api`、`jobs`、`admin-api`、`payment-callback`；四个函数于 2026-09-15 反查均为 `Deployment completed`，远端 `seed` 保持已删除 |
| CloudBase 定时任务 | `jobs` 已确认存在每分钟触发器 `shiguang-jobs-every-minute` |

这些结果证明当前 SQL 线上读链路和函数运行链路可用，但不代表已经完成微信开发者工具真机、真实支付或支付通知验签验证。

## 继续开发时的操作顺序

1. 按 `docs/数据库初始化与权限.md` 管理 SQL 表结构变更；不要再为业务创建文档数据库集合。
2. 运行 `npm run prepare:functions` 后重新部署函数；部署时保留 CloudBase 中的服务端数据库密码配置。
3. 新环境首次使用时，在身份认证创建用户名密码用户并取得 UID，再由受控终端设置 `BOOTSTRAP_OWNER_UID` 执行 `npm run admin:bootstrap-owner`；后台通过 CloudBase 会话调用 `api` 云函数，不在浏览器拼接 Token。
4. 按 `docs/微信支付接入清单.md` 确认商户号与 AppID 绑定、支付权限和风控状态正常后，先做真机小额支付/查单，再做真实退款到账闭环。
5. 用验收矩阵覆盖并发、竞态、越权、故障恢复和报表口径。

## 尚未完成

- 店主管理会话已真实使用；技师首次账号创建/登录与完整 STAFF 权限验收仍待完成。
- 原环境早期初始化时创建的 8 个文档数据库集合仍保留在原环境中，但当前业务代码不再读取它们；按“禁止直接删除文件/数据”的约束暂不清理。
- 商户号与 AppID 绑定、支付权限和风控状态仍需在微信商户平台解除当前手机端“商户异常”后确认。
- 真机支付、支付成功回调、查单、关单、用户取消退款、人工确认未到店退款和退款到账回调仍需真实资金端到端验收。
- MySQL 备份/恢复演练、10/30/100 并发与安全验收。
- 图片云存储上传已实测；每周模板发布后的真实账号回读、通知模板实际申请与发送仍待验证。
- Azure HTTPS API 接入方案和正式发布。
- 小程序隐私保护指引中的“手机号”声明、主体认证/手机号能力，以及真实手机体验版授权闭环尚未完成验收。

## 重要注意事项

- 不要把 AppSecret、私钥、Token、密码或完整 `.env` 内容提交到项目或文档。
- 小程序前端不应执行 SSH，也不应直接暴露服务器凭据。
- “代码上传成功”不等于“功能可用”；修改后必须在模拟器或真机实际调用关键链路。
- 微信支付当前处于“商户凭证、微信支付公钥、回调入口和资金状态机均已接入，但商户异常尚未解除、真实交易闭环未验收”状态；完成真机闭环前不能据此对外收款。
